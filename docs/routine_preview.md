# 미국장 프리뷰 루틴 — 지시문 (2026-09-17 확대판)

> 클라우드 루틴 "미국장 프리뷰"의 프롬프트는 이 파일을 읽고 그대로 따르라는 몇
> 줄뿐입니다. 규칙을 고칠 때는 여기를 고치고 커밋하세요.

## 임무

평일 21:30 KST에 **그날 미국장의 메인 글** 한 편을 써서
`editorial/previews/us_<KST 오늘 날짜>.json`에 커밋합니다. 커밋되면 `preview_publish.yml`이
바로 발행합니다 — 본진(fermata.it.kr)에는 **비공개**로 올라가고, 이 맥이 20분 안에 네이버 블로그에
**본문 전문을 바로 공개**합니다. 독자가 읽는 곳은 네이버입니다. 22시가 지나면 가치가 줄어드는
글이라 사람 검수를 기다리지 않습니다. 그래서 **관문을 통과한 것만 커밋합니다.**

**2026-09-17부터 이 글이 메인입니다**(사장님 결정 "1번2번3번 진행"). 아침 마감 시황(07:20)은
그대로 있고 "어제 판정·성적표·한국 연결"을 맡습니다. 프리뷰는 **독자가 돈을 걸기 전에 읽는
글**입니다 — "오늘 밤 무엇이 나오고, 어떤 결과가 나올 수 있고, 그때 무엇을 보고, 우리는 어떻게
하는가".

- 분량 **4,500~6,000자**, **12절**, 그림 **8~10장**, 출처 **4곳 이상**.
- **22:00까지 커밋**합니다. 개장은 22:30(서머타임 해제 뒤 23:30)입니다. 9/15 실험에서
  같은 부피의 글이 19분 걸렸습니다 — 21:49에 끝났습니다. 22:00을 넘길 것 같으면 **절을 줄이지
  말고 그림을 줄이십시오**(22:10이 넘으면 그림 6장으로 커밋하고 보고에 "시간 초과로 그림
  6장"이라고 적습니다 — 그때는 `--graphics 6`으로 관문을 돌립니다). 22:15가 넘으면 그날 글은
  포기하고 보고합니다.

왜 이렇게 바꿨나(2026-09-17 조사): 같은 자리(이벤트 전날 밤)의 글에서 재테크농부는 6,243자 +
월가리포트 18건 + "저는 이렇게 대응하겠습니다"를 냈고, 우리 프리뷰는 1,135자·3절로 표에서 가장
얇은 글이었습니다. 9/15 합본 실험에서 루틴이 그 밤에 13절·5,884자·출처 5곳을 냈으니 부피는
됩니다. 남은 것은 아래 12절 가운데 **시나리오 표·초보자 상자·대응 원칙·월가 리포트** 네 절입니다 —
그게 재테크농부 「FOMC 대응 전략」과 우리의 차이였습니다.

## 쓰지 않는 날

- 오늘 밤(미국 동부 시간 오늘)이 미국 증시 휴장일이면 쓰는 대신 보고하고 종료합니다.
  WebSearch로 "NYSE holidays 2026"을 확인합니다.
- `editorial/previews/us_<오늘>.json`이 이미 있으면 **알림 없이 조용히** 종료합니다(정상입니다).
- 어제 미국장 원고도, 재료 파일도, 확인된 일정도 없으면 보고하고 종료합니다.
  **억지로 채우지 않습니다.**

## 읽을 것

1. `docs/editorial-style.md` 전부 — 제목 문법(「더한 축 여섯」 포함)·「판단」의 **대응 원칙**·낱말·리듬.
2. 어제 미국장 원고 `editorial/us_<가장 최근 날짜>.json`의 `outlook`과 `closing.check`(확인 지점),
   그리고 **어제 프리뷰** `editorial/previews/us_<어제>.json`의 `closing.check` — 오늘 2절이 그것을 판정합니다.
3. `data/engines_us_<KST 오늘>.txt` — `story_material.yml`이 21:10 KST에 커밋합니다. 안에 있는 것:
   `[밸류에이션]`(우리가 보는 종목의 FWD PER·목표가 괴리·52주), `[의견 변경]`(워치리스트),
   **`[월가 리포트]`**(시장 전체의 오늘 등급·목표가 변경, 15~30건 — 2026-09-17 추가), `[실적 일정]`,
   `[내부자 매수]`, `[기관 보유 변동]`(13F), `[미 거시]`(FRED), `[계절성]`. 없으면
   `python -m src.story_engines all --market us > data/engines_us_<오늘>.txt`를 시도하되 커밋하지 않습니다.
4. `data/price_us_<가장 최근>.json` — 어제 종가. **가격대 숫자는 여기서만.**
5. `data/price_kr_<KST 오늘>.json` — 오늘 한국장 종가(10절 "내일 아침 한국장"에 씁니다).
6. 형식 예시: `editorial/features/us_2026-09-06_deere_upgrade.json`(스키마). 절 구성은 아래 표대로.

환경은 `docs/routine_common.md`의 "이 샌드박스에서 할 수 있는 것" 절과 같습니다.

## 절차

1. `pip install -r requirements.txt && apt-get install -y -qq fonts-nanum`(한글 폰트가 없으면 그래픽 렌더가 첫 번에 실패합니다)
2. **재료를 먼저 읽습니다** — 어제 원고, 재료 파일, 시세 파일. 그다음 WebSearch로 오늘 밤 일정을
   **서로 다른 출처 2곳 이상**에서 확인합니다(실적 발표 회사·개장 전/마감 후, 경제지표 시각·컨센서스·직전치,
   연준 인사 발언). 시각은 한국시간(서머타임 중 동부 +13시간, 11월 첫째 일요일 이후 +14시간).
3. **12절을 이 순서로 씁니다.** 절마다 **400자 이상**(관문이 봅니다). 소제목 32자 이하, 절반쯤 명사구.

   | # | 절 | 무엇을 쓰나 | 재료 | 그래픽 |
   |---|---|---|---|---|
   | 1 | 어젯밤 한 줄 | 지수 넷 + "안쪽"(업종·변동성·금리). 지수만 보면 평범해도 안이 시끄러운 밤이 있습니다 | 시세 파일 | `number_cards` |
   | 2 | 어제 확인 지점, 오늘 판정 | 어제 프리뷰와 어제 시황의 `closing.check`를 하나씩 — 맞음·빗나감·미정, 숫자로. "그때 우리가 이렇게 말했고 → 이렇게 됐고 → 그래서 오늘은" | 어제 원고 | — |
   | 3 | 오늘 밤 일정 표 | 시각(KST)·항목·컨센서스·직전치. 실적은 회사·발표 시점·EPS 컨센서스 | WebSearch 2곳 + `[실적 일정]` | `calendar_strip` |
   | 4 | **나올 수 있는 결과 셋** | 오늘 밤 핵심 이벤트의 결과를 세 갈래로(예: 인상+매파 점도표 / 인상+온건 / 동결). 갈래마다 "시장이 놀라는 것"과 "먼저 볼 지표"를 한 줄씩. **예측이 아니라 조건입니다** | — | `fact_table`(3행 표) |
   | 5 | 왜 그렇게 되나 | 핵심 지표 하나를 차트로(물가·금리·유가). **초보자 설명:** 문단 필수 | `[미 거시]` FRED | `price_history` 또는 `rate_compare` |
   | 6 | 시장은 이미 얼마나 반영했나 | 페드워치·금리선물·포지셔닝. "발표 후 30분 안에 먼저 볼 것"(예: 2년물 4.6%) 하나를 못 박습니다. **초보자 설명:** 선반영 | WebSearch | — |
   | 7 | 주인공 종목 | 오늘 밤 실적·재료가 걸린 종목 1~2개: FWD PER·목표가 괴리·52주 고점 대비, 13F·내부자 있으면 함께 | `[밸류에이션]` `[기관 보유 변동]` `[내부자 매수]` | `valuation_bars` 또는 `stock_spotlight` |
   | 8 | **월가 리포트** | `[월가 리포트]`에서 **8~15건**을 표로: 종목·증권사·행동·등급 전→후·목표가 전→후. 그중 우리 종목·오늘 밤 주인공에 걸린 **3~5건은 출처를 열어 '핵심 내용' 한 줄**을 붙입니다(WebSearch: `<증권사> <종목> price target`). 못 찾으면 붙이지 않습니다 — 지어내지 않습니다 | `[월가 리포트]` | `fact_table` |
   | 9 | 서학개미 보유 상위 | 우리가 보는 미국 종목 가운데 어젯밤 크게 움직인 것과 밸류에이션 위치 | `[밸류에이션]` 시세 파일 | `movers_list` |
   | 10 | 내일 아침 한국장 | 오늘 한국장에서 이미 반영된 것(장비주·반도체·환율)과 오늘 밤 결과가 내일 어디로 넘어가는지 | `price_kr_<오늘>` | `price_history`(코스피) |
   | 11 | 오늘 밤 볼 것 셋 | "X가 나오면 Y를 본다" 조건문 셋. 자기 전에 정해 두는 것 | — | `checklist` |
   | 12 | Fermata's Take + **우리 대응 원칙** | 판단 세 문장 + **구간별 대응 원칙**(예: "발표 전에는 서두르지 않는다 / 발표 후에는 주가보다 2년물·수급을 본다 / 급락이면 이유부터 구분 — 이익 전망이 그대로면 나눠서 볼 후보, 주문까지 줄면 판단을 바꾼다"). 사라·팔라·목표가는 쓰지 않습니다(「판단」의 경계). 끝에 `closing.check`(내일 아침 시황이 판정할 확인 지점) | — | — |

   - **그래픽 8~10장, 같은 종류는 2장까지**(9/15 실험에서 `number_cards`가 네 번 나왔습니다). 표지(`cover`)는 별도.
   - **초보자 설명 둘 이상**(관문). 재테크농부 FOMC 글은 bp·점도표·2년물·선반영 넷에 달았습니다. 개념이 나오는
     절 바로 아래 `초보자 설명:`으로 시작하는 문단.
   - 원고 필드: `kind` "feature", `series` "프리뷰", `date` KST 오늘, `slug` `us-<date>-preview`,
     `category_id` 121(Daily), `tags`(오늘 밤 주인공 회사·지표 2~3개), `related` 2개, `ko.title`, `ko.narrative` 12절,
     `ko.closing`(heading `Fermata's Take`, `body`, `check: {"due": <내일>, "what": ...}`), `graphics` 8~10장
     (`{"kind": "cover", "featured": true, ...}` + 절마다 `section` 번호). 인자 형식은 `src/feature_graphics.py`.
   - 본문에 날짜(`N월 N일`)를 씁니다. 마크다운 볼드(`**`) 금지, 강조는 `<b>…</b>`.
4. **제목.** `ko.title`은 **앞을 보는 제목**입니다 — 오늘 밤 무엇이 무엇을 정하는지. 어젯밤을 요약하는
   제목(`은행주 이틀째 급락, 반도체는 오히려 웃었습니다`)은 시황 제목이지 프리뷰 제목이 아닙니다 — 2026-09-17
   첫 확장판이 그렇게 나가 사장님이 짚었고, 관문이 **시간 축(오늘 밤·내일·새벽) 또는 독자 축(보세요·볼 것)**을
   늘 요구합니다. `python -m scripts.recent_titles preview`를 먼저 읽고 "이번 제목에 넣을 것"의 축을 채웁니다
   (시간·질문·독자 가운데 최근 네 편에 없는 것 — 관문이 막습니다). 프리뷰는 시간 축이 자연스럽지만
   질문·독자·1인칭 축도 돌려 씁니다 — `오늘 밤 이것 하나만 보세요`, `이 반등, 오늘 밤도 이어질까?`,
   `우리가 FOMC 전에 현금을 남기는 이유`. 답 노출형(`…, 이유는 B입니다`)은 다섯 편에 한 번.
   **후보 셋 이상을 축을 달리해 쓰고** `python -m scripts.title_pick preview "후보1" "후보2" "후보3"`으로 점수를 본 뒤
   **가장 높은 것을 `ko.title`에, 후보 전부를 `ko.title_candidates`에** 적습니다(2026-09-18, 사장님 "a진행" —
   관문이 같은 계산으로 확인하고, 후보가 없거나 더 먼 후보가 있으면 막습니다). 후보는 셋 다 나가도 되는 제목이어야 합니다.
5. **관문.** `python -m src.feature_gate editorial/previews/<파일>.json --graphics <그래픽 수>` — 통과 전에는
   커밋하지 않습니다. → `WORDPRESS_URL= WORDPRESS_USERNAME= WORDPRESS_APP_PASSWORD= python -m src.publish_feature editorial/previews/<파일>.json --render-only`
   → `output/features/<slug>/`의 그림을 `Read`로 봅니다(축을 한 종목이 독차지하거나 이름이 막대와 어긋나는 것은
   코드만 봐서는 안 보입니다) → 제목·소제목·그래픽 글자만 따로 읽습니다.
6. `git add editorial/previews/<파일>.json && git commit -m "프리뷰: <제목>" && git push origin HEAD:main`
   push 뒤 워크플로를 3분 넘게 기다리지 않습니다. **알림은 보내지 않습니다**(맥이 네이버에 올린 뒤
   네이버 주소로 보냅니다). 건너뛰었거나 실패했을 때만 휴대폰 알림 한 번.

## 절대 규칙

- 숫자는 시세 파일·재료 파일·확인한 기사에서만. 일정 시각은 출처 2곳이 맞을 때만.
- 예측하지 않습니다. 시나리오 표와 대응 원칙은 **조건문**입니다 — "X면 Y를 본다".
- 월가 리포트의 '핵심 내용'은 원천에서 읽은 것만. 없으면 등급·목표가 줄만 싣습니다.
- 한 편만 씁니다. 커밋하는 것은 원고 JSON 하나뿐입니다(`output/`·`data/` 커밋 금지).
- 22:00 마감. 넘기면 그림을 줄이지 절을 줄이지 않습니다. 22:15면 포기하고 보고합니다.

## 완료 보고

원고 경로, 제목, 12절 소제목, 그래픽 장수와 종류, 월가 리포트 건수(그중 핵심 내용을 붙인 건수),
출처 4곳, 관문 출력 전문, 커밋 해시, **시작 시각과 커밋 시각**. 건너뛴 날은 이유.
