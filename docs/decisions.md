# 규칙의 내력 — 왜 그렇게 정했나

`CLAUDE.md`(= `AGENTS.md`)에는 **규칙만** 남기고, 규칙이 생긴 사건·날짜·사장님 말·실측은 2026-09-25에 이 파일로 옮겼다.
절 이름과 불릿 순서는 `CLAUDE.md`와 같다 — 규칙 문장의 굵은 첫머리로 찾으면 그 규칙의 이야기가 여기 있다.

왜 갈랐나: `CLAUDE.md`는 모든 세션과 클라우드 루틴에 자동으로 실린다(2026-09-25 실측 3만 6천 자). 이야기까지 매 턴 실을
이유가 없고, 규칙만 남겨야 규칙이 읽힌다. 다만 이야기를 지우면 같은 실수를 다시 겪는다(9/4 제목 규칙처럼 "왜 그랬는지"를
잊으면 낱개 규칙만 덧붙이게 된다). 그래서 지우지 않고 옮긴다.

쓰는 법: 규칙을 새로 적을 때는 규칙 문장을 `CLAUDE.md`와 `AGENTS.md`(네 절 동일, `tests/test_rule_files.py`)에, 사건·날짜·
인용을 여기에 같은 자리에 적는다. 규칙을 지울 때는 `python -m scripts.rule_diff CLAUDE.md`로 사라진 줄을 뽑아 줄마다 말한다.

---

## 검증 규칙 (반드시 지킬 것)

- **한국어 시황을 작성하거나 고치기 전에 `docs/editorial-style.md`를 읽고 따른다.**
  영어 시황 관용구를 직역하거나 시장을 불필요하게 의인화하지 않는다. 완성된
  구조화 원고는 `src/editorial_quality.py`의 검사를 통과해야 한다.

- **제목·소제목 규칙은 `docs/editorial-style.md`의 「제목 문법」·「소제목」 절 하나뿐이고
  블로그의 모든 글(시황·기준표·프리뷰·가이드)에 같다. 검사도 `src/editorial_title.py`
  하나다(2026-09-08 전면 개정·통합). 2026-09-09에 옛 규칙을 전부 버리고 **사장님이 고른
  예문집**(제목 35개 `OWNER_PICKS`, 시황 소제목 세트 S-A6, Checkpoint 세트 S-C1)으로 다시
  시작했다 — 재테크농부 통계로 만든 규칙 문장은 쓰지 않는다. 취향은 예문으로, 사실·화면은
  장치로(등락률 반올림·둘 이상, 꼬리표, 길이, 금지 낱말, 같은 틀 반복).** 글 종류별로 규칙을 따로 두지 말 것 — 나뉘어
  있으면 고칠 때마다 한쪽이 빠진다(사용자 지적). 사용자가 세 번(9/4·9/6·9/8) 지적했다. 원인은
  9/4에 옛 표본(199개)으로 적은 "첫 절은 종목 이름 + 등락률로 연다"였고, 루틴이 그대로
  따라 `A 8.47% 급등, B 5.78% 급락. 코스피가 0.58% 하락한 이유.`가 매일 나왔다.
  낱개 규칙을 덧붙이는 식으로 고치지 말 것 — 뼈대 규칙이 그대로면 결과도 그대로다.
  **루틴은 `python -m src.editorial_gate <원고>`를 통과한 뒤에만 커밋한다.** 발행
  단계는 문체·제목·구조를 막지 않고 경고만 남긴다("발행을 멈추게 하지 말고 처음부터
  제대로 쓰게 하라", 2026-09-08) — 숫자 대조(`editorial_facts`)만 막는다.
  **네 번째 지적(2026-09-09, "여전히 제목과 소제목은 똑같구나")의 원인은 낱개 규칙이 아니라
  틀의 반복이었다** — 시황 제목은 대비형·이유형뿐, Checkpoint 일곱 편 중 다섯이 `…에 갈린다`
  (사람이 지음), 소제목은 열에 아홉이 `~습니다`. 그래서 관문이 최근 글과 뼈대를 대조해 막는다
  (`editorial_title.frame_issues`·`heading_mix_issues`, 「제목 문법」 11·「소제목」). 낱개 규칙을
  더 얹지 말고, 같은 지적이 또 오면 **반복되는 틀**부터 찾을 것. 쓰기 전에 `python -m scripts.recent_titles
  <kr|us|checkpoint|preview>`로 최근 다섯 편의 꼴과 "피할 것"을 읽는다.
  **다섯 번째 지적(2026-09-18, "제목 상태 왜 이래 적용안된거같은데")의 원인은 대조 범위였다** — 규칙은 다
  돌았고 통과했는데 목록별(한국장·미국장·프리뷰 따로)로만 봐서, 독자가 보는 네이버 피드에는 '은행주'가 셋,
  '3년 2개월 만의 금리 인상'이 둘, 대비 꼴이 둘 나란히 나갔다. 그래서 대조 목록은 `src/title_feed.py`의
  **독자 피드 한 줄**(올라가는 시각 순 — 미국장 D는 한국 D+1 아침)이고, 뼈대에 더해 **같은 주인공은 다섯 편에
  둘까지·같은 어구(7자 이상)는 한 번**을 본다(`editorial_title.subject_issues`). 규칙을 검사할 때는 "무엇을
  보나"만이 아니라 **"무엇과 대조하나"**가 독자의 화면과 같은지 본다.
  **다섯 번의 공통 원인은 문의 종류였다(2026-09-18 "왜 자꾸 재발하는지", 사장님 "a진행")** — 관문은 "안 되는 것"을
  거르는 문인데 쓰는 쪽(루틴)은 그 문을 넘는 **최소**를 찾는다(예문 35개 중 가장 쉬운 하나가 28편 중 11편, 축 규칙
  뒤에는 `…까?`가 7편 중 5편). 금지 규칙을 더 얹어도 다음 낮은 곳으로 같은 일이 난다. 그래서 문을 **고르는 문**으로
  바꿨다: 원고에 제목 후보 셋 이상을 축을 달리해 적고(`ko.title_candidates`), 관문(`editorial_title.candidate_issues`)이
  독자 피드와의 거리 점수(`title_distance`)가 가장 높은 후보를 제목으로 요구한다. 루틴은 쓰기 전에
  `python -m scripts.title_pick <kr|us|preview|checkpoint|…> "후보1" "후보2" "후보3"`으로 같은 점수를 본다. 대상은 독자
  피드의 글 전부(시황·프리뷰·기준표·주간·이벤트, `CANDIDATE_KINDS`), 가이드는 뺀다. **제목 지적이 또 오면 규칙 문장을
  더하지 말고 점수표(`title_distance`)의 가중치와 후보 요구를 먼저 본다.**
  **버린 꼴은 소제목에도 같다**(2026-09-18, "1번 진행해" — 9/18 소제목 「외국인이 돌아왔습니다」가 제목 관문만 피해 나갔다).
- **보이는 부분만 깎지 않는다 — 바꾸기 전에 연쇄 목록, 바꾼 뒤에 원본 대조**(2026-09-17, 사장님: "사과를 깎아
  달라고 했는데 내 눈에 보이는 부분만 깎아 놓고 나머지는 안 깎은 식으로 일 처리한다"). 같은 날 실제로 있었던 일 셋 —
  ① 9/15 네이버 '본문 전문'으로 바꿀 때 본문 변수 하나만 갈아 끼우고 그림 상한·사진·outlook·출처 경로를 안 봤다
  ② 프리뷰 지시문을 12절로 다시 쓰며 「앞을 보는 제목」 줄을 빠뜨렸고 그날 밤 루틴이 어젯밤 요약형 제목을 냈다
  ③ 제목 하나를 바꾸는 데 표지 그림(제목 문장이 박혀 있다)·네이버 제목·네이버 첫 그림이 딸려 있었다.
  그래서 절차 셋을 **의무**로 둔다.
  - **연쇄 목록**: 무엇을 바꾸기 전에 그것을 읽는 곳을 `grep`으로 다 찾아 표로 만든다(본진·네이버·표지·
    테스트·문서). 표의 줄마다 '바꿈'·'해당 없음'을 적어 보고에 넣는다. 목록이 없으면 바꾸지 않는다.
  - **원본 대조**: 옮기거나 다시 쓴 것은 기계로 대조한다 — 네이버 글은 `python -m scripts.naver_audit`,
    원고→네이버 블록은 `tests/test_naver_completeness.py`, 문서·지시문을 다시 쓰면
    **`python -m scripts.rule_diff <파일>`로 사라진 규칙 줄을 뽑아 보고에 열거하고 줄마다 '일부러 뺐다'고 말한다.**
    말할 수 없는 줄은 도로 넣는다. 사장님이 정한 문구는 테스트 바늘(`tests/test_preview_series.py`의 needle 등)로 박는다.
  - **보고의 '안 한 것' 칸**: 확인 못 한 것·손대지 않은 것을 보고에 따로 적는다. 비어 있으면 안 한 것이 없다는 뜻이다 —
    빈칸을 채우려 들지 말고, 진짜 없을 때만 비운다.
- **사용자는 코드를 읽지 않는다. 보고에는 원인·설명·제안을 함께 담는다**(2026-09-08).
  같은 지적이 두 번 나오면 증상을 고치지 말고 **그 결과를 만드는 규칙**을 찾아 바꾼다.
  보고는 쉬운 말로 넷을 적는다 — 무슨 일이 있었나, 왜 그랬나, 무엇을 바꿨나, 사용자가
  정할 것은 무엇인가. 실제로 있었던 일: 9/4에 적은 제목 규칙("종목 이름 + 등락률로
  연다")이 매일 같은 제목을 만들었는데, 세 번 지적받는 동안 낱개 규칙만 덧붙였고
  사용자가 "대체 어떤 규칙이 들어 있길래"라고 물은 뒤에야 원인을 찾았다. 사용자의
  말: "오늘 알지 못했다면 계속해서 시간을 낭비했을 것이다." 규칙을 바꾼 뒤에는 **다음
  결과물을 직접 보고 벤치마크와 비교한 것**을 보고한다 — "고쳤다"가 아니라 "이렇게
  나왔다"가 보고다.
- **이미지는 쓰기 전에 실제로 다운받아 눈으로 확인한다.** alt text나 검색어만
  보고 판단하지 말 것. 실제로 있었던 일: Unsplash에서 "Korean won
  currency"/"Korean won banknote"로 검색했는데 결과가 중국 위안화(마오쩌둥
  초상) 사진이었다. alt text는 "assorted banknotes"처럼 그럴듯해 보였지만
  실제 사진은 완전히 틀렸다. 대표 이미지(featured image)든 본문 삽입
  이미지든, 발행 전에 `Read` 툴로 다운받은 파일을 직접 봐야 한다.
- **통화/지수 같은 추상적 개념은 이미지 검색이 특히 안 맞을 확률이 높다.**
  브랜드/제품처럼 사진으로 명확히 알아볼 수 있는 대상(개별 종목명, 회사
  로고)은 검색이 비교적 안전하지만, "원화", "코스피", "금리" 같은 추상
  개념은 사진 자체가 없거나 다른 나라 것이 섞여 나올 때가 많다.
  `src/main.py`의 `_featured_image()`가 이 이유로 watchlist(개별 종목)만
  대상으로 하고 macro(지수·환율·금리·원자재)는 일부러 제외한다 — 이 원칙을
  다른 이미지 자동화에도 유지할 것.
- **워드프레스 관리자 화면(플러그인 설정 등)의 정확한 위치는 기억으로
  추측하지 말고 WebSearch로 확인한 뒤 안내한다.** 실제로 있었던 일: Rank
  Math의 OpenGraph 썸네일 설정 위치를 세 번 잘못 추측했다가(존재하지 않는
  탭 이름들) 검색해서야 정확한 위치("Titles & Meta → Global Meta" 탭)를
  찾았다. 게다가 알고 보니 그 설정은 이미 올바르게 되어 있었다 — 확인
  없이 추측한 게 완전히 시간 낭비였다.
- **디버깅할 때 원인을 추측만으로 단정하지 말 것.** 실제로 있었던 일: 발행이
  안 됐던 원인을 GitHub Actions 실행 시간(4초 만에 실패)만 보고 "yfinance가
  클라우드 IP를 막았을 것"으로 추측해서 안내했는데, 실제 로그를 보니 전혀
  무관한 Anthropic 콘솔 API 크레딧 소진이 원인이었다. 근거가 간접적일 때는
  "이건 추측이다"라고 명시하고, 가능하면 실제 로그/트레이스백을 요청하거나
  직접 확인한 뒤 결론을 내릴 것.
- **관리자 화면 경로를 안내하기 전에, 반드시 REST API로 현재 상태를 먼저
  조회한다.** 조회로 확인되지 않은 화면 위치는 안내하지 않는다. 실제로 있었던
  일(2026-09-01): 메타태그에 `google-site-verification`이 없는 것만 보고 "구글
  서치콘솔 미등록"이라 단정했는데, `wp/v2/plugins`를 한 번만 조회했으면 Site Kit
  by Google이 이미 연결돼 있는 걸 알 수 있었다. 이어서 검색 결과로 랭크매쓰 메뉴
  경로를 안내했지만 실제 화면과 달라 사용자가 헤맸고, Site Kit API의 403을
  "권한 부족"으로 오독했으나 실제로는 조회에 쓴 계정에 구글 인증이 없었을 뿐이다.
  세 번 모두 원인은 **부분적 근거로 추측한 뒤 확인된 사실처럼 보고한 것**이다.
  - 먼저 조회할 것: `wp/v2/plugins`(설치·활성 플러그인), `wp/v2/settings`,
    `google-site-kit/v1/core/modules/data/list`(연결 상태), `pll/v1/settings`
  - 확인 불가능한 항목은 "확인 못 했다"고 말하고, 사용자에게 한 번만 보면 되는
    구체적 확인 지점을 요청한다.
- **REST 액션 이름을 추측해서 호출하지 말 것.** 실제로 있었던 일: 랭크매쓰
  사이트맵 캐시를 비우려고 `rankmath/v1/toolsAction`에 액션 이름을 추측해서
  연달아 POST했다. 그 엔드포인트에는 설정 초기화 같은 위험한 액션도 있어서,
  운이 나빴으면 라이브 사이트를 망가뜨릴 수 있었다. 문서로 확인된 형식만 쓰고,
  아니면 사용자에게 넘긴다.
- 화면(홈페이지, 발행된 글, 템플릿 변경사항)을 눈으로 확인해야 할 때는
  Playwright 헤드리스 브라우저(`playwright` pip 패키지 + Chromium, 이미
  이 컴퓨터에 설치돼 있음)로 스크린샷을 찍어서 실제로 본 뒤 결과를
  보고한다. curl로 HTML 구조만 확인하고 "됐다"고 하지 말 것 — 구조가
  맞아도 실제로 보면 다를 수 있다(예: 페이지 제목이 두 번 겹쳐 보이는
  버그는 curl로는 안 보이고 스크린샷으로만 발견됨).
  - NinjaFirewall이 헤드리스 브라우저 User-Agent를 봇으로 차단할 때가
    있다 — 그럴 땐 실제 브라우저 User-Agent 문자열을 지정하면 통과된다.
  - 워드프레스 템플릿/글을 수정한 뒤에는 항상
    `POST /wp-json/wp-super-cache/v1/cache {"delete_cache": true}`로
    캐시를 지워야 변경사항이 바로 보인다.

## 비용/자동 발행 원칙

- 기본 자동 발행은 `src/generate_free.py`를 사용하며 외부 생성형 AI API를
  호출하지 않는다. 실제 시세와 RSS 헤드라인만 정해진 형식으로 조합한다.
- 평일 16:20 한국장 실행은 `--en`으로 한국어판과 영어판을 함께 만든다.
  `src/generate_free_en.py`가 같은 가격 데이터에서 영어 문장을 직접 작성하고,
  두 언어 품질 검사가 끝난 뒤 워드프레스 업로드 단계로 넘어간다.
- **규칙 기반 생성 결과(`src/main.py` / `generate_free.py`)는 절대 바로 공개하지
  않는다.** 이 경로는 검수용 초안까지만 만든다. `run(publish_live=True)`는 예외로
  멈추게 해 뒀으니 그 가드를 풀지 말 것. 2026-09-02에 한 번 예약 실행을 바로
  공개했다가 되돌린 이력이 있다 — 카드에 있는 숫자를 바로 아래 문단에서 다시
  나열하고, 나스닥이 1% 넘게 빠진 날 멕시코 증시·드롭박스 기사를 붙이고,
  마무리 문단이 다른 날과 한 글자도 다르지 않은 글이 그대로 공개됐다.
- **`editorial_quality` 통과를 공개 근거로 삼지 말 것.** 문장 길이·금지 표현 같은
  형식 검사이지 내용이 그날 장세를 설명하는지 보지 않는다. 위 글도 검사는 통과했다.
- **`editorial_facts`가 원고의 숫자를 시세와 대조한다**(`publish_editorial`이 한국어·영어
  양쪽에 돌린다). 두 가지를 본다 — 원고에 적힌 종목 등락률이 `price_data`와 맞는지,
  그리고 그날 절대 등락 1위 종목이 원고 어디에도 없지는 않은지. 후자는 2026-09-02
  미국장 원고가 15.81% 오른 델을 한 번도 언급하지 않고도 형식 검사를 통과한 데서
  나왔다. 장중 인용("오전 9시 44분 기준 1.55%")·보유율·어림수는 일부러 건너뛴다 —
  확실할 때만 실패시키는 것이 이 검사의 원칙이다. 이 예외를 좁혀 오탐을 만들지 말 것.
  **2026-09-26 보강**(사장님: "9번 제외 모두 고쳐"). 점검 때 9/23 원고에 '삼성전자 2.70% 하락'(실제 +2.70%), 두 종목 숫자를
  맞바꾼 문장, 다음 문단에 '장중'이 있는 틀린 숫자, '시가총액' 문장을 넣어 보니 전부 통과했다. 원칙이 '확실할 때만'이어서
  방향과 뒤바뀜은 설계 때 다루지 않았고, 건너뛰기 창(앞 20자·뒤 45자)이 문장·문단을 넘었다. 지난 원고 1,502자리를 다시
  대조해 보니 실제로 틀려 나간 것은 없었다 — 아직 안 터진 구멍이었다. 고친 뒤 87편을 다시 돌려 새로 막힌 맞는 글이 없음을
  확인했다(새로 걸린 1건은 9/23 프리뷰가 옛 기준 1.36%를 인용한 실제 불일치). 그 과정에서 오탐 셋을 잡아 규칙에 넣었다 —
  'Group'의 'up', GOOG('Alphabet')·GOOGL('Google') 같은 한 회사의 두 주식, 영어 'respectively' 문장.
- **발행 뒤에는 되읽어 확인한다**(`publish_wordpress.verify_published`). 2026-09-03
  아침에 워크플로가 초록 체크로 끝났는데 한국장 글은 올라가지 않았고, 알아챈 것은
  사람이 사이트를 열어봤기 때문이었다. 상태·제목·본문 길이가 기대와 다르면 예외로
  멈춰 워크플로가 빨간 X로 끝나게 한다.
- **`publish_check.yml`이 발행 시각 40분 뒤에 그날 글이 실제로 사이트에 있는지
  확인한다**(`src/check_publication.py`). `verify_published`는 발행 스크립트가
  돌았을 때만 확인하므로, 루틴이 원고를 못 써서 아무것도 커밋되지 않은 날은
  이 워크플로가 잡는다. 실패하면 깃허브가 메일을 보낸다. 휴장 판정은 달력이 아니라
  지수의 실제 마지막 거래일이고, 글의 신선도는 **"거래일 이후에 수정됐는가"**로 본다 —
  전에 쓰던 "36시간 안에 수정" 창은 주말·휴장(2026-09-07 노동절) 다음 아침에 새
  거래일이 없어도 헛경보를 냈다. `tests/test_check_publication.py`가 그 아침을 재현한다.
- **테스트는 `tests.yml`이 push마다 돌린다.** 발행이 자동인데 테스트가 수동이면
  의미가 없다. 외부 호출이 필요한 테스트는 mock으로 막아 이 워크플로에서 돌 수 있게
  유지할 것.
- **바로 공개해도 되는 것은 조사·집필을 마친 `editorial/*.json` 원고뿐이다.**
  원고 커밋(push)으로 `editorial_publish.yml`이 공개한다. 사람이 원고를 검수한 뒤
  커밋하는 것이 곧 검수 단계다.
- 수동 실행(`workflow_dispatch`)과 손으로 돌리는 실행은 종전대로 임시저장이며,
  이 기본값(`publish_draft(status="draft")`)을 바꾸지 말 것.
- **`market_brief.yml`은 시세 수집·커밋·루틴 호출만 한다(2026-09-07부터).**
  `python -m src.main --fetch-only`로 돌려 규칙 기반 초안은 만들지 않는다 — 그
  초안은 규칙상 공개되지 않는 부산물인데 매일 워드프레스 draft로 쌓여 관리자
  화면에서 "데이터만 나열한 글"로 오인됐고(실제로 그렇게 보고받았다), 초안
  검사가 죽으면 시세 커밋까지 같이 죽었다(2026-09-04). 시세 파일이 그날 처음
  커밋되면 같은 실행이 루틴의 **API 트리거**(`ROUTINE_<MARKET>_FIRE_TOKEN`
  시크릿, 루틴별 `/fire` URL)로 조사·집필 루틴을 즉시 깨운다. 루틴의 자체
  예약(cron)은 한 시간 뒤 예비다 — 2026-09-07에 자체 예약이 2시간 8분 늦게 떠서
  글이 19:05에 나간 것이 계기다. 호출 실패는 워크플로를 실패시키지 않는다.
  `tests/test_workflows.py`·`tests/test_fetch_only.py`가 이 구조를 고정한다.
- **루틴 샌드박스 네트워크는 전체 열림(Full)이다**(2026-09-07 저녁, 사용자가 직접
  설정). 실측(점검 루틴 `trig_017rvrGfkDE55t3f5fGSGQ5N`, 12:30 UTC) — 열림:
  `api.unsplash.com`·`images.unsplash.com`·`finance.naver.com`·
  `query1.finance.yahoo.com`·`fermata.it.kr`·`api.openverse.org`·
  `commons.wikimedia.org`·`opendart.fss.or.kr`·`ecos.bok.or.kr`·`api.stlouisfed.org`·
  `pypi.org`·`api.github.com`. **`m.stock.naver.com`만 전체 열림에서도 000**이다 —
  네트워크 정책이 아니라 그쪽에서 막는 것으로 보이고(추측), 한국장 편입 종목
  수집(`fetch_movers`)이라 루틴이 자구책으로 시세를 받는 날만 영향이 있다. 설정은
  **루틴 편집 → 지시문 아래 구름 아이콘(Default) → 톱니 → Network access**에 있고
  (문서: code.claude.com/docs/en/routines), 바꾼 뒤에는 점검 루틴을 `RemoteTrigger
  run`으로 다시 돌려 확인한다. claude.ai 설정의 "기능 → 네트워크 송신 허용" 토글은
  채팅 분석 도구용이라 루틴과 무관하다. 루틴의 사진 검색은 `--source unsplash`로
  고정한다(품질 기준 — Openverse가 열렸다고 바꾸지 않는다). 루틴 지시문은
  `docs/routine_common.md`·`routine_kr.md`·`routine_us.md`에 있고 루틴 프롬프트는
  그 파일을 가리키는 몇 줄뿐이다 — **규칙은 파일에서 고친다.** 같은 환경(Default)의
  환경변수에 `UNSPLASH_ACCESS_KEY`·`FRED_API_KEY`·`ECOS_API_KEY`가 들어 있다
  (2026-09-07 점검 루틴으로 세 API 인증까지 확인). 값은 사용자가 환경 편집
  대화상자(Environment variables, `.env` 형식)에 넣었고 저장소에는 없다. 점검 루틴은
  네트워크와 환경변수를 함께 본다.
- **매체 목록은 `config/magazine_feeds.yaml` 하나다**(2026-09-14). 레이더가 읽는 묶음(`sets`:
  `magazine` 18곳·`ko` 9곳)과 시황 루틴이 이름만 쓰는 목록(`market_media.kr`·`.us`)이 함께 있다.
  전에는 시황 매체가 `docs/routine_kr.md`·`routine_us.md` 본문에 적혀 있어, 매체를 더하려면
  문서를 고쳐야 하고 어느 글이 어디서 오는지 한눈에 안 보였다. **아침 레이더
  (`python -m scripts.magazine_radar --set <묶음>`)는 주말 Checkpoint(`ko,magazine`)·한국어 가이드
  (`ko`)·영어 가이드(`magazine`)·잡지(`magazine`)에만 붙인다** — 시황과 프리뷰에는 붙이지 않는다.
  주제가 이미 그날 장세로 정해져 있고 마감 직후에 나가야 하는 급한 글이라 얻을 것이 없다.
  레이더에서 얻는 것은 **주제뿐이다**(가이드에서는 목록 안 순서만 바꾼다) — 기사를 옮겨 쓰지 않고
  사실은 늘 출처에서 다시 확인한다. 피드를 더할 때는 **직접 받아 보고 출력까지 읽는다**:
  2026-09-14에 한국 매체 26곳을 재어 한국경제(403)·이데일리(연결 거부)·서울경제(404)를 버렸고,
  열리지만 연예·스포츠가 절반인 전체 섹션 피드도 버렸다. 죽은 피드와 잡음은 둘 다 "오늘 화제가
  없다"로 보인다.
- **검색에 막힌 시황 매체는 헤드라인 피드로 본다**(2026-09-26, 사장님: "막히는곳이 다 신뢰할만한 뉴스매체다
  해결방안을 찾아라" → "합친 방식으로 진행해"). 루틴의 검색 도구(WebSearch)는 `allowed_domains`에 Anthropic 로봇을
  막은 매체가 하나라도 있으면 `400 The following domains are not accessible to our user agent`로 검색 전체를
  거절한다. 매체마다 따로 검색해 보니 시황 매체 14곳 가운데 Reuters·WSJ·Barron's·연합뉴스·매일경제가 막혀
  있었고(MarketWatch·AP·FT도 막힘, 뉴스1·뉴시스·파이낸셜뉴스는 열림), 9/26 미국장 루틴이 이 오류로 두 번
  헛돌았다. 지시문이 "`market_media`를 `allowed_domains`에 넣으면 잡음이 준다"고 적어 둔 것이 원인이었다.
  다섯 곳은 버리지 않았다 — 연합뉴스·매경·WSJ(다우존스)는 공식 RSS, Reuters·Barron's는 구글뉴스 `site:` RSS로
  제목·시각·요약을 받는다. 셋을 한 벌로 했다: 받는 길을 매체 목록에 적고(`domain`·`search`·`feeds`), 그 목록을
  읽는 `scripts/media_headlines.py`를 precheck ⑩으로 붙이고(루틴 턴을 늘리지 않는다), 지시문을 고쳤다. 출력은
  최근 18시간(한국)·24시간(미국), 같은 기사 한 줄, 우리 종목·시장 낱말 제목을 앞에, 매체당 8·6줄 — precheck
  전체가 약 5,500자다. **본문은 열지 않는다** — 9/26에 이 맥에서 연합·매경 본문은 200으로 열렸지만 매체가 AI
  접근을 막은 것이므로 로봇 이름을 감추지 않고(`UA`) 제목만 단서로 쓰며, 사실 확인은 검색되는 매체에서 한다.
  피드 실측: `feeds.a.dj.com`(WSJ 옛 주소)은 200인데 2025년 1월 기사에서 멈춰 있었고 Barron's 자체 RSS는 403.
- **영어 가이드의 주제는 뉴스가 아니라 서치콘솔 검색어가 정한다**(2026-09-14, 사용자 승인 "1번 2번 진행해").
  아침 레이더(뉴스)를 영어 가이드에 붙였다가 도로 뗐다 — 실측으로 잡지 묶음 78줄에 한국 기사가 **0줄**이었다.
  당연한 결과다. 영어 가이드는 **상시 검색 글**이고 순위를 정하는 것은 검색 수요·경쟁·페이지 신뢰도이지
  오늘 기사가 아니다. 그래서 이 맥의 `~/.market-brief-google/gsc_queries.py`가 매일 10:30 KST(launchd
  `kr.it.fermata.gscqueries`)에 검색어를 긁어 `data/search_queries.json`으로 커밋하고, 루틴이
  `python -m src.search_queue`로 "오늘 무엇을 할지"를 읽는다(루틴은 서치콘솔에 로그인할 수 없다 —
  `data/benchmark_stats.json`과 같은 방식). **주 7편은 새 글 5편 + 기존 글 고치기 2편이다** — 8위를
  4위로 올리는 것이 새 글의 색인을 기다리는 것보다 확실하고, 2026-09-14 기준 색인 11 · 미색인 32였다.
  첫 수집에서 `kospi trading hours`가 노출 6에 **게재순위 66위**였다(전용 글을 갖고 있는데도). 큐의
  `gain`은 순서를 매기려고 만든 어림치이니 **글에 적지 말 것** — 업계 CTR 곡선이지 우리가 잰 값이 아니다.
  영어 가이드에서 레이더(`--set en_kr`)는 주제가 아니라 **상시 글이 낡았는지**를 보는 자리로만 쓴다.
  **2026-09-18(사장님 "2번 진행"): 큐는 검색 의도를 본다** — `search_queue.intent`가 행동(how to·buy·broker·etf·tax·hours)
  1.3배, 정의(what is·difference) 0.7배. 정의형은 AI 개요가 답을 먼저 보여 줘 1위여도 클릭이 적다. 순위 밀린 글은
  제목·첫 소제목·첫 문장에 검색어를 그대로 넣는다(거래시간 65위·계좌개설 43위 글을 그렇게 고쳤다). `related` 제목은
  발행 때 살아 있는 제목으로 바뀐다(`publish_feature._refresh_related`) — 손으로 적은 제목이 페이지마다 달랐다.
- **한국어 시황은 본진에 `private`로 올리고, 네이버에는 본진 글을 그대로 옮긴다**(2026-09-16, 사용자 지시:
  "예전처럼 본진에도 한국어 시황 다 올리고 비공개로 발행해 / 네이버에는 본진 글을 똑같이 옮기기만 해라").
  스위치는 `publish_editorial.KO_DAILY_TO_WORDPRESS`(켬)와 `KO_DAILY_STATUS`(`private`) 둘이다.
  영어판(`-en`)은 그대로 공개다. **2026-09-15에 "본진에 아예 안 올린다"로 바꿨다가 하루 만에 되돌린 것이다** —
  되돌린 이유를 반드시 남긴다: 네이버로 옮기는 코드(`scripts/naver_post.py`)가 **처음부터 `outlook`·
  `insight_section`·`sources`를 집어 가지 않았는데**, 본진이 공개였을 때는 거기서 읽혀 손해가 보이지 않았다.
  본진을 닫자 그 셋이 **아무 데도 실리지 않았다**(하루 800~1,500자, 그중 `insight_section`이 그날 조사가
  가장 많이 들어간 부분이다). 같은 조사에서 나온 나머지 셋도 같이 고쳤다 — ① 그림을 **절 번호**로 붙인다
  (전에는 `GRAPHIC_PREFERENCE` 종류 순서로 다시 줄을 세워 앞 네 장만 붙였고, 그래서 국채금리 절 밑에
  원익홀딩스 카드가 붙었다) ② 4장 상한을 없앴다 ③ `editorial_gate`가 본문·인사이트 **사진도 파일로 남긴다**
  (전에는 세기만 해서 네이버에 한 장도 못 갔다). **교훈: 원본이 어디인지 바뀌면 옮기는 코드의 전제도 같이
  바뀐다 — 본문을 어디서 가져오는지(`full`)만 갈아 끼우고 끝내지 말 것.**
- **프리뷰는 네이버에 본문 전문(링크 없이)으로 나가고, 본진에는 비공개로만 올린다**(2026-09-15, 사용자 지시).
  네이버 쪽은 시황과 같은 처리이고(`naver_post`의 `is_daily or preview`), 본진 쪽은
  `publish_feature.LIVE_STATUS = {"프리뷰": "private"}` 한 줄이다. 워드프레스 `private`는 글·주소·분류를
  그대로 두고 **사이트맵·목록·피드·익명 접근(404)에서만 뺀다** — 실측으로 확인했다. 같은 글이 두 곳에
  공개돼 네이버가 유사문서로 거르는 일을 막는 것이 목적이고, 시황(본진 발행 자체를 끔)보다 한 단계 약한
  처리다. 이미 올라가 있던 6편도 그날 비공개로 바꿨다. 되돌리려면 `LIVE_STATUS`에서 한 줄을 지운다.
  **비공개 글은 텔레그램·스레드에 알리지 않는다** — 그 주소가 독자에게 404이기 때문이다. 알림은 맥의
  동기화가 네이버에 올린 뒤 네이버 주소로 보낸다(`scripts/notify_naver_post.py`, 시황과 같은 경로).
- **미국장 시황·프리뷰를 저녁 한 편으로 합치는 개편은 2026-09-15에 시도했다가 보류했다**(사용자 결정).
  되돌린 커밋은 `11c6ac6`의 revert다. 그날 확인한 것은 기록으로 남긴다 — 문턱을 절 11·시각자료 8·
  출처 4로 올리자 루틴이 **11절·그래픽 11장·본문 4,680자·출처 6곳**을 실제로 냈고 관문을 통과했다
  (벤치마크 중앙값 4,596자). 새 절 넷(어제 확인 지점 판정·서학개미 보유 점검·오늘 한국장·내일 아침
  연결)도 다 들어왔다. 남은 문제는 셋이었다 — ① 목표 5,500~6,500자에 못 미쳐 절당 얕았다
  ② `number_cards`가 한 글에 네 번 쓰여 단조로웠다 ③ 루틴이 19분 걸려 22:30 개장 전 여유가 적었다.
  시간대 근거(서학개미 골든타임 밤 11시~새벽 1시, 07:20은 독자가 자는 시간)는 그대로 유효하므로,
  다시 할 때는 이 세 가지부터 손보면 된다.
- **`publish_check.yml`은 예비 예약까지 끝난 뒤(19:00/10:00 KST)에 돈다.** 루틴
  보다 먼저 울리면 정상 발행일에도 실패 메일이 온다(2026-09-07 17:35).
- **코스피·코스닥 일봉은 개별 종목보다 늦게 나온다**(2026-09-08 실측: 18:30 KST까지도
  FinanceDataReader의 KS11·KQ11이 전날 행에 머물렀고 종목은 그날 종가였다). 그날
  16:29·16:37·16:41 워크플로와 17:40 예비 루틴이 전부 "기준일 불일치"로 멈춰 한국장
  글이 빠졌다. `fetch_kr._apply_final_index_quote`가 네이버 실시간 `ms=CLOSE` 값을
  **전일 종가 + 등락폭 = 확정 종가** 등식이 맞을 때만 오늘 행으로 덧붙인다(휴장일엔
  등식이 안 맞아 가짜 행이 생기지 않는다). `tests/test_price_fetch.py`의
  `LaggingIndexDailyBarTest`가 이 날을 재현한다. 등식 검증을 빼고 "CLOSE면 붙인다"로
  느슨하게 만들지 말 것.
  **2026-09-10에는 일봉이 사흘(09-07)째 멈춰** 하루 보정으로는 못 메웠고 다시 세 번 실패했다. 그래서
  일봉이 우리가 이미 커밋한 시세 파일보다 뒤처지면 그 파일의 이력(그날그날 네이버 확정값으로
  덧붙인 것)을 밑바탕으로 쓰고 등식은 그 마지막 종가와 맞춘다(`_latest_committed_index`).
- **한국장 종목 가격·등락률의 기준은 KRX 정규장 확정 종가이고, 시세 파일은 거래일마다 한 번만 쓴다**(2026-09-25, 루틴
  정밀 분석). 그전에는 FinanceDataReader 일봉의 오늘 행(16:20~16:40 스냅숏 — NXT 애프터마켓이 20:00까지 움직인다)을 전날
  NXT 포함 종가로 나눠, 삼성전자 9/23이 2.70%로 나갔다(KRX 3.62%, NXT 확정 3.24% — 그날 27종목 중 25종목이 어느 기준과도
  안 맞았다). 언론·HTS·네이버 현재가가 쓰는 기준은 KRX 정규장이라 그것으로 바꿨다 — `fetch_kr._apply_krx_close`가 네이버
  폴링(`SERVICE_ITEM`, `nv`/`pcv`, `ms=CLOSE`일 때만)으로 덮어쓰고, **폴링의 `cr`·`cv`는 부호가 없다**(KB금융 실데이터로 확인 —
  부호는 `nv−pcv`에서). 같은 날 :20/:27/:34 재수집과 다음 날 재수집이 값을 매번 바꿔 커밋해 루틴 push를 거절시키고(시황
  8건 중 7건, 숫자 재확인에 13~30턴) 발행된 글과 파일을 어긋나게 했으므로(9/24 프리뷰 "0.09% 오른 7,080.9"), 이미 있는
  거래일 파일은 가격을 두고 **빈 칸(수급·편입 종목)만 보강**한다(`main._merge_price_file`). 되돌리지 말 것. 첫 실전은 9/28.
- **종목별 수급은 다음 날 아침 전 거래일 파일에 채운다**(2026-09-26, 사장님: "9번 제외 모두 고쳐"). 9/26 점검에서 한국장
  시세 파일의 종목별 수급이 9/11~9/22 여덟 파일 모두 0/27이었다. 9/11~9/16은 옛 HTML 표가 리다이렉트돼 조용히 비었고
  (9/18 수정 커밋은 이를 '9/17·9/18 이틀'로 알았다), 9/18부터는 날짜가 틀린 값을 일부러 버렸는데 그 뒤를 메운다던
  '다음 날 재수집'은 평일엔 거래일이 바뀌어 새 파일을 쓰므로 한 번도 돌지 않았다(9/25 '검증'은 추석 연휴 날이었다).
  그사이 한국장 글의 종목별 수급 그림(flow_compare·flow_chart)은 9/10까지 8편 중 7편에 있다가 9/11부터 0편이 됐다.
  네이버 API가 최근 15거래일을 주므로 `fetch_rows`로 여러 날을 받아 **날짜가 맞는 줄**을 고르고, 아침 미국장 수집이
  `scripts/fill_kr_flows.py`로 전 거래일 파일의 빈 칸만 채운다. 비어 있던 여덟 파일도 같은 날 되채웠다. 그날 글은
  종목별 수급을 쓸 수 없으므로 전 거래일 것을 날짜를 밝혀 '어제'로 쓰고, 그림은 `"day": "previous"`로 그린다.
- **루틴 턴 절약 장치 넷**(2026-09-25 감사, 실행 13건 실측 중앙값 126턴·22분): ① 첫 명령 `python -m scripts.routine_precheck
  <kr|us|preview>`가 시각·시세·원고 유무·재료·**어제 원고 요약**·최근 제목·그래픽 인자·금지 낱말을 한 화면에 낸다(어제 원고
  JSON 통째 읽기가 실행당 가장 큰 낭비였다) ② 관문·렌더가 그림을 **모음판**(`sheets/sheet-NN.png`, 원본 크기 그대로 서너 장씩)
  으로 이어 붙여 낱장 11장 대신 서너 장만 Read한다 — **읽는 장수를 줄이지는 않는다**(눈으로만 잡힌 오류가 11건 중 7건)
  ③ 그 7건의 꼴을 코드가 본다(`graphic_checks.collect_spec_issues`에 `price_data`·`section_body` — 제목의 '만·유일·신고가·최고'
  vs 데이터, 그린 종목이 절 본문에 하나도 없음, 표 셀 폭; `fact_table`은 19→17→15로 자동 축소) ④ 프리뷰에도 숫자 대조
  (`editorial_facts.collect_issues_for_preview`, 목표주가·52주 고점·PER·프리마켓·%포인트는 제외)와 제목 후보의 어림수
  ('5% 넘게' vs 3.88%) 검사. 지시문에는 push 뒤 대기·조회 금지, 검색은 사실당 두 번, `env` 출력 금지(Unsplash 키가 실행 로그에
  평문으로 남았다 — 키 교체는 사장님이 2026-09-25 저녁에 "그냥 둬"로 정했다: 계정 안에만 남았고 위험이 낮다)를 넣었다. 관문을 **매일 걸리게** 만들지 말 것 — 종목 목록 그림의 '절반 넘게 본문에 없음'은
  기존 59장 중 30장이 걸려 참고로만 둔다.
- 예약 시각을 마감 정각으로 되돌리지 말 것. 16:00/07:00 정각은 시세가 아직
  안 채워져 `USD/KRW: 결측값(NaN)`으로 죽는다(실제로 3회 연속 실패했고, 7분 뒤
  수동 실행은 성공했다). 그래서 두 시장 모두 20분 뒤로 미뤄 뒀다.
- **대표 이미지는 `src/featured_image.py`가 세 가지 중 하나를 고른다** — 사진(photo)·
  단독(single)·삼분할(trio). 그날 주인공이 하나로 뚜렷하고 그 종목의 업종 사진이
  보관함에 있으면 사진, 없으면 단독, 여럿이 함께 움직였으면 삼분할이다. 어느
  쪽이든 **숫자는 실제 마감 시세에서 나오므로 틀린 그림이 붙지 않는다.**
- **표지 사진은 `config/photo_pool.yaml`에 미리 승인해 둔 것만 쓴다.** 시황은 사람
  검수 없이 자동 공개되므로 발행 시점에 검색하면 안 된다 — `korean bank`가 삼청빌라,
  `korean won banknote`가 중국 위안화로 나온 실측 기록이 있다. **검색은 사람이 미리
  하고 자동 실행은 고르기만 한다**(`src/photo_pool.py`). 보관함의 처음 20장은 2026-09-06에
  Openverse에서, 그다음 45장은 2026-09-08에 Unsplash에서 받아 `Read` 툴로 한 장씩
  보고 고른 것이고(업종당 다섯 장 안팎 — 같은 종목이 사흘 연속 주인공이어도 표지가
  다르게), 명세의 `seen` 줄에는 검색어가 아니라 **사진에 실제로 보이는 것**을 적는다.
  - 사진을 더할 때: `python -m src.photo_search --sheet <이름> <검색어들>` → 대조표를
    `Read`로 열어 보고 → `assets/photos/`에 넣고 → 명세에 항목을 적는다.
  - 업종에 사진이 없는 날은 **사진 없이 그래픽으로 간다.** 억지로 아무
    사진이나 붙이면 보관함을 만든 이유가 없어진다.
  - **동적 편입 종목에는 붙이지 않는다**(아래 워치리스트 항목과 같은 이유).
  - 같은 업종에 사진이 여럿이면 (2026-09-08엔) 날짜로 돌려 썼다 — 삼성전자가 사흘 연속 주인공이어도
    표지는 사흘 다 다르다. 같은 날 다시 돌리면 같은 사진이 나온다(재실행이 표지를
    바꾸면 워드프레스에 미디어가 중복으로 쌓인다).
    **2026-09-26에 횟수 기반으로 바꿨다.** 사장님이 네이버 표지를 쭉 보고 "인텔쪽 사진만 단조롭게 계속 들어간 것 같다 …
    다양하게 다채롭게 돌려쓰기로 한 거 아니었냐"고 했다. 실측: 최근 40편 중 사진 표지는 7편, 그중 인텔(미국장 9/17)과
    SK하이닉스(한국장 9/22)가 같은 남색 기판 — 날짜÷5의 나머지가 우연히 같았다. 규칙 문장("사흘 연속 다르게")은 글자 그대로
    지켰지만 뜻(다채롭게)은 못 지킨 것. 상태 파일 없이 커밋된 시황 원고의 price_data로 그 묶음이 사진을 쓴 횟수를 다시 세어
    이력을 만들어 가장 오래 안 쓴 장을 고른다(`featured_image.cover_history`, 2026-09-15부터; 처음엔 횟수÷장수였는데 같은 날
    반도체 사진 4장을 더하자 순서가 밀려 방금 쓴 남색 기판이 또 걸려 바로 바꿨다). 같은 날 네이버 표지도 Checkpoint·프리뷰·가이드에
    원고의 Unsplash 사진을 붙이게 했다 — 9/12에 네이버 표지를 `01-cover` 그래픽으로만 집어 33편이 같은 틀이었다. 한 일은
    전부 페이블(같은 세션 계보)이 했다.
    처음엔 사진이 있으면 틀을 뺐는데 사장님이 "남색베이지 틀도 우리 그때 당시 엄선했던 작품이다 함부로 버릴수 없다"고 해
    같은 날 **사진 → 틀 → 본문**으로 확정했다("1번으로 해, 사진 다음에 틀"). feature-style의 "사진이 나오면 대체한다" 문장은
    워드프레스 대표 이미지 한 장을 고르는 순서로만 남겼다. 이미 올라간 네이버 글은 건드리지 않는다(사장님: "네이버 자꾸
    만지면 안 좋다고 했잖아") — 다음 글부터다.
  - CC BY 계열은 **저작자 표시가 의무**라 `credit`을 미디어 캡션에 그대로 싣는다.
    NC·ND는 `photo_search`가 애초에 거른다.
- **`src/fetch_images.py`(Unsplash -> 위키미디어 엔티티 검색)는 이제 자동 발행 경로에서
  쓰지 않는다**(2026-09-07). 위키데이터 P18의 함정은 기록으로 남긴다 — 이름으로
  검색만 하면 '카카오'가 카카오 열매(페루 아마존 사진)로, 위키백과 '신한지주' 대표
  이미지가 숭례문으로 나온다(둘 다 실측). **검색어로 사진을 자동으로 붙이는 경로를
  다시 만들지 말 것.**
- **인사이트 스토리 사진은 사람이 본 사진에서만 온다(2026-09-07).** 경로는 둘뿐이다
  — 루틴이 샌드박스에서 Unsplash 후보를 직접 내려받아 `Read`로 보고 골라 원고의
  `image.url`에 적은 것, 아니면 `image_query`의 코어 종목명으로 승인 풀
  (`config/photo_pool.yaml`)에서 고른 업종 사진. 발행 시점 실시간 검색은 뺐다 —
  아무도 안 보고 붙는 유일한 경로였고 'SK Hynix memory chip'에 로고가 나갔다.
  코어 종목명 가드(`_concrete_image_query`)는 그대로다: "korean won banknote"에
  중국 위안화, "red traffic light"에 초록불이 나온 실제 사례가 근거다. 종목명이
  없거나 풀에 업종이 없으면 사진 없이 표·차트만 나가고 이유가 로그에 남는다.
  `tests/test_insight_photos.py`가 이 구조를 고정한다.
- **워치리스트는 두 시장 모두 코어(고정) + 동적(그날 편입) 2단 구조다.** 코어는
  `config/watchlist_kr.yaml`의 섹터 대표 21종목과 `config/watchlist_us.yaml`의
  16종목이고, 여기에 그날 거래대금 상위 종목이 `source: "dynamic"`으로 붙는다
  (`src/fetch_movers.py`; 한국장은 네이버 금융, 미국장은 나스닥 스크리너).
  전에는 고정 목록뿐이라 2026-09-03처럼 보험·은행이 주도한 날 KB금융·신한지주를,
  2026-09-02 미국장에서는 15.81% 오른 DELL을 데이터로 담지 못했다. 동적 편입
  기준은 **거래대금**이며 등락률로 바꾸지 말 것 — 등락률 상위는 써니전자·레버리지
  ETN 같은 것들이 올라온다(실측).
- **동적 편입 종목에는 사진을 붙이지 않고**(위 가드), **기준일이 코어와 다르면
  아예 버린다**(`fetch_kr`/`fetch_us`의 `_fetch_dynamic_tier`). 이름도 모르는 종목 하나가 그날
  발행 전체를 멈추게 하면 안 되기 때문이다. 반대로 대표 이미지의 LARGEST MOVE는
  편입 종목도 후보에 넣는다 — 데이터에서 텍스트를 그리는 것이라 틀린 그림이 붙을
  위험이 없고, 라벨이 "LARGEST WATCHLIST MOVE"라 더 크게 움직인 종목을 빼면
  라벨이 사실과 어긋난다.
- GitHub Actions에는 **Anthropic·OpenAI 키를 전달하지 않는다.** 유료 생성
  경로(`generate_post.py`, `translate_post.py`)를 자동 실행에 다시 연결하지 말 것.
  Unsplash 키는 Actions 어디에도 더 이상 넘기지 않는다(2026-09-07) — 발행 워크플로의
  실시간 사진 검색을 뺐기 때문이다. 사진 검색은 루틴 샌드박스(네트워크가 열린 뒤)
  에서 사람처럼 보고 고르는 경로로만 한다.
- **조사·집필은 클라우드 루틴이, 시세 수집은 GitHub Actions가 맡는다.** 루틴
  샌드박스는 네이버·야후·KRX·언론사 RSS가 네트워크 정책에 막혀 있고(2026-09-02
  실측: CONNECT 403) WebSearch만 열려 있다. 그래서 예약 실행이 시세를
  `data/price_<market>_<거래일>.json`으로 커밋하고, 루틴이 그 파일을 읽어
  원고를 쓴다. 루틴 프롬프트에서 `fetch_kr/fetch_us`를 직접 부르게 만들지 말 것 —
  그렇게 두면 매일 같은 지점에서 멈춘다.
- 무료 생성 결과는 `output/{market}_{trading_date}_generated_free.json`에
  캐시된다. 자동 검증하지 못한 시장 원인·전망은 추측해서 넣지 않는다.
- **`publish_editorial.py --render-only`는 `WORDPRESS_*`가 설정돼 있으면
  완전한 dry-run이 아니다.** 글 내용은(이미 공개된 거래일이면) 안전장치로
  안 바뀌지만, 본문 그래픽·사진은 실제로 미디어 라이브러리에 업로드된다.
  2026-09-06에 이미 발행된 날짜로 렌더 확인만 하려다 안 쓰는 미디어가
  남았다. 순수 렌더만 보고 싶으면 `WORDPRESS_URL` 등을 비우고 돌린다
  (`publish_feature.py --render-only`는 이 문제가 없다 — 업로드 코드 자체가
  `render_only`일 때 안 불린다).

## 기준표(feature) 글

2026-09-06에 이 글 한 편을 올리는 데 **두 시간**이 걸렸다. 대부분은 글을 쓰는
시간이 아니라 **도구를 못 찾아 헤맨 시간**이었다. 아래 다섯 줄이 그 두 시간을
줄이려고 만든 것이다. 순서대로 쓴다.

    python -m src.story_engines --list                 # 1. 재료를 먼저 뽑는다
    python -m scripts.title_helper 대비                # 2. 제목은 실제 예문에서
    python -m src.photo_search --sheet 은행 bank ...   # 3. 사진은 한 판에 12장
    python -m src.feature_gate <원고> --graphics <N>   # 4. 검사는 이거 하나
    python -m src.publish_feature <원고>               # 5. 발행도 이거 하나

- **글을 쓸 때 우리는 재테크농부다.** 벤치마크 100편(2026-08-06~09-05)을 실측해
  정한 기준이 `docs/feature-style.md`에 있다. 기준표 원고를 쓰거나 고치기 전에
  반드시 읽는다. 일간 시황 기준(`editorial-style.md`)과 별개다.
- 핵심 셋만 여기 적어 둔다. **제목에 결론을 넣지 않는다**(읽기 전에 답을 알면
  누를 이유가 없다), **시각자료 최소 6장**(벤치마크 중앙값 10장), **본문에 확인
  날짜를 박는다**(글의 수명이 그날까지 늘어난다).
- **독자에게 보이는 이름은 Checkpoint다**(2026-09-09, 사용자 결정). 내부 키(`series: 기준표`)는
  그대로 두고 화면에 나가는 곳만 바꿨다 — 워드프레스 분류 Checkpoint(id 432)와 홈 탭, 글
  머리말 `Checkpoint · N월 N일까지 확인할 것`(원고 최상위 `deadline`, 관문이 확인), 표지
  kicker. 제목 글자에 라벨을 넣지 않는다 — 대신 확인 날짜를 제목에 넣는 것이 기본이다.
- **제목·소제목 규칙은 `docs/editorial-style.md` 「제목 문법」·「소제목」 하나이고 모든
  글(시황·기준표·프리뷰·가이드)에 같다. 검사도 `src/editorial_title.py` 하나다**
  (2026-09-08). 글 종류별로 규칙을 따로 두지 말 것 — 나뉘어 있으면 고칠 때마다
  한쪽이 빠진다.

### 재료를 먼저 뽑는다 (`src/story_engines.py`)

- 엔진 12개가 밸류에이션·등급변경·실적일정·내부자매수·수급·계절성·13F·업종·
  금리(FRED/ECOS)를 가져온다. **재료 없이 쓰기 시작하지 않는다** — 오늘 글이
  막힌 이유는 문장이 아니라 재료였다. `src/source_check.py`가 서로 다른 외부
  출처 3곳 미만이면 발행을 막는다(벤치마크 100편에서 출처 53종·인용 989건을
  세어 정한 하한이다).
- **엔진이 `⚠ N건을 받지 못했습니다`를 찍으면 그 결과를 쓰지 않는다.** DART가
  연결을 끊는 중에 `매수 0건`이라고 보고한 적이 있다. 없는 사실을 쓰게 된다.

### 제목·소제목은 실제 예문에서 가져온다 (`scripts/title_helper.py`)

- 관계(`대비`·`원인`·`질문`·`경고`·`개수`·`시한`)로 벤치마크 실제 제목을 찾아
  **그 어법을 그대로 갈아입힌다.** 낱말을 세어 새로 짓지 않는다.
- **빈도는 그 낱말이 어느 자리에 오는지를 알려주지 않는다.** `반대로`가 224회
  나오길래 `금리는 반대로 갔다`라고 썼는데, 224회가 전부 문장 첫머리 접속사였고
  술어로 쓴 예는 1회였다.
- **문서에 적힌 수치를 목표로 삼지 않는다.** `1. 지금 숫자`라는 소제목이 나온
  것은 문서의 "소제목 14자"를 맞추려다 생긴 일인데, 그 14자는 폰트 크기로
  소제목을 찾던 옛 코드가 낸 값이라 **애초에 틀린 수였다.**
  `scripts/compare_to_benchmark.py`는 이제 코퍼스를 그 자리에서 다시 재고,
  결과를 "맞춰야 할 기준이 아니다"라고 못박아 출력한다. 코퍼스는 이 컴퓨터에만
  있으므로(남의 글을 저장소에 올리지 않는다) 클라우드 루틴은 `--export-stats`로
  내보낸 집계 파일 `data/benchmark_stats.json`을 읽는다 — 2026-09-07까지는 루틴에서
  코퍼스를 못 찾아 매일 우리 수치만 찍고 그걸 대조 결과처럼 보고했다. 이 집계표는
  이 맥의 야간 작업(`scripts/bench_nightly.sh`, launchd 23:10)이 수집 직후 새로
  내보내 집계값이 바뀐 날만 커밋·푸시한다(2026-09-08). 맥이 꺼져 있던 날은 다음
  실행이 채운다.

### 사진 (`src/photo_search.py`)

- **한 판에 12장을 펼쳐 놓고 번호로 고른다.** 한 장씩 들이밀다 여섯 번 퇴짜맞은
  날이 오늘이다. `--sheet`가 검색어 여러 개를 한 번에 돌려 대조표를 만든다.
- **그 회사 사진이 없으면 한 단계 넓힌다.** 은행주 글이면 `bank`·`financial
  district`, 반도체 글이면 `microchip`·`semiconductor`. 특정 회사가 아니어도
  업종이 맞으면 표지로 충분하다. **회사가 식별되는 사진도 괜찮다** — 은행주가
  무너진 글에 은행 사진이 나오는 것은 당연하다.
- 출처는 Openverse(워드프레스 재단 CC 통합검색, 키 불필요)이고 `NC`·`ND`는
  거른다. 유니스플래시 한국어 검색과 위키데이터 P18은 **믿지 않는다**
  (`korean bank`→삼청빌라, 신한은행 P18→숭례문, SK하이닉스→포장재. 전부 실측).
- 받은 파일은 **반드시 `Read` 툴로 직접 본다.** 예외 없다.

### 검사와 발행은 각각 하나뿐이다

- `src/feature_gate.py`가 다섯 검사(`editorial_quality`·`editorial_title`·
  `feature_checks`·`source_check`·벤치마크 대조 둘)를 한 번에 돌린다. 앞의 넷은
  막고, 벤치마크 대조는 보고만 한다. **검사를 따로따로 기억해서 돌리지 않는다** —
  오늘 빼먹어서 세 번 되돌아갔다.
- `src/publish_feature.py`가 렌더→그래픽→멱등 업로드→발행/갱신→되읽기를 한
  명령으로 끝낸다. 임시 스크립트를 새로 쓰지 않는다. 오늘 그렇게 하다가 공개된
  글을 초안으로 되돌리고, 이미지를 `-2.png`·`-3.png`로 쌓고, 카테고리를 새로
  만들었다.
- **주말 기준표는 루틴이 쓴다**(2026-09-08, `docs/routine_feature.md`). 토·일 09:00
  KST에 재료를 뽑아 원고를 커밋하면 `feature_draft.yml`이 올린다. **2026-09-12부터
  자동화 스위치가 켜져 있어 커밋 즉시 공개된다**(사용자: "체크포인트는 자동발행으로 바꿔주고
  바로 오늘부터"). 스위치를 끄면 임시저장으로 돌아가고, 그때 공개는 사람이 "발행"이라고 한 뒤
  `python -m src.publish_feature <원고> --publish` 한 번이다. 루틴이 고른 표지 사진은
  `featured_photo.url`(대조표를 눈으로 보고 고른 Unsplash 주소)로 적고 러너가 받는다 —
  `output/`은 커밋하지 않는다.
- **시황의 판단은 글 안에서 닫힌다**(2026-09-08). 원고의 `closing.check`(확인 지점)를 다음 거래일 글의
  「어제 본 것」 절이 읽어 판정하고 `review`(어제 판정)로 적는다. 관문(`editorial_gate`)이 세 문장 이상의
  판단·확인 지점·어제 판정·초보자 설명을 본다(`src/editorial_judgment.py`). 발행은 막지 않고 경고만 남긴다.
  **성적표는 2026-09-26에 없앴다**(사장님: "2 완전히 깨끗하게 없앤다"). 9/8에 "계좌인증 대신 우리가 판단에
  책임지는 방식"으로 만들었다 — 기준표·시황의 확인 지점을 `data/scoreboard.yaml`에 모아 날짜가 지나면 판정을
  적고 /scoreboard/ 페이지(724)로 내보냈다. 9/26 점검에서 셋이 드러났다. ① 페이지는 한 번도 공개되지 않았다
  (임시저장이라 방문자에게 404였고, 어느 화면에서도 링크되지 않았다). ② 판정 43건 가운데 28건이 '절반'이었다 —
  확인 지점 한 줄에 두세 가지를 묶고 방향을 정하지 않아, 무엇이 나와도 반쯤 맞았다. ③ 18일 동안 성적표 커밋이
  71번이었고, 발행 뒤 페이지 갱신 경고가 매일 한 줄씩 났으며, 9/23에는 성적표 단계가 발행 워크플로를 빨간 X로
  끝냈다. 글 안의 확인·판정은 성적표가 아니라 전날 원고를 읽으므로 그대로 남겼다. 월요일 보고서의 적중률 줄은
  같이 빠졌다. 되살린다면 확인 지점부터 방향이 있는 한 문장으로 바꿔야 판정이 뜻을 갖는다.
- **자동화 스위치는 GitHub 저장소 변수 `FERMATA_AUTO_PUBLISH` 하나다.** `true`면 기준표
  초안이 커밋 즉시 공개된다. **2026-09-12부터 `true`다.** 끝까지 자동화하는 것이 목표이므로 새 발행 경로를
  만들 때는 이 변수를 같은 뜻으로 읽게 만든다 — 스위치를 여럿 두지 않는다. 성적표 페이지도 이 변수를 읽었으나
  2026-09-26에 성적표와 함께 없앴다.
- **밤 9시 반 미국장 프리뷰는 시리즈 "프리뷰"로 기준표 파이프라인을 쓰되, 2026-09-17부터 그날 미국장의 메인 글이다**
  (사장님: "1번2번3번 진행하고 월가 의견 밀도 재테크농부 만큼 늘리고 9/15일에 보류한거 다 진행해"). 평일 21:30 KST
  루틴이 `editorial/previews/us_<날짜>.json`을 커밋하면 `preview_publish.yml`이 본진에 **비공개**로 올리고 맥이 네이버에
  전문을 올린다. 구성은 `docs/routine_preview.md`의 12절 — 어젯밤 한 줄·어제 판정·일정 표(KST·컨센서스·직전치)·
  **결과 시나리오 표**·왜 그렇게 되나(초보자 상자)·시장의 선반영·주인공 종목(밸류에이션 엔진)·**월가 리포트**·서학개미
  보유·내일 아침 한국장·볼 것 셋(조건문)·Take + **우리 대응 원칙**. 문턱은 절 10·시각자료 8·출처 4
  (`editorial_title.SECTION_FLOORS`, `feature_checks.SERIES_LIMITS`, `source_check.SERIES_MIN_SOURCES`)에 더해 9/15 합본
  실험에서 드러난 셋을 막는다 — 절당 400자, 같은 그래픽 종류 2장, 초보자 설명 둘 이상. 시간은 지시문의 **22:00 마감**
  (실측 19분). 아침 마감 시황은 그대로다(판정·한국 연결 담당) — 합치지 않는다. 근거는 2026-09-17 조사: 같은
  자리(이벤트 전날 밤) 글에서 재테크농부는 6,243자·월가리포트 18건·구간별 대응 계획을 냈고 우리 프리뷰는 1,135자·3절로
  표에서 가장 얇았다. 9/15 실험 글(13절·5,884자·출처 5)이 루틴이 그 밤에 낼 수 있다는 증거다.
  **「월가 리포트」 엔진**(`story_engines.street`, 2026-09-17): 워치리스트 16종목만 보던 `ratings`(하루 1~2건)와 별개로
  MarketBeat 오늘 페이지에서 시장 전체의 등급 전→후·목표가 전→후를 읽는다(50행/쪽, 하루 15~30건). 막히면 야후로
  S&P 500을 돌린다(목표가 없음). '핵심 내용'은 원천에 없으니 루틴이 상위 몇 건만 출처를 열어 한 줄로 붙인다 —
  지어내지 않는다. `story_material.yml`의 `all --market us`에 들어 있다.
- **주말 편성**(2026-09-12, 사용자 결정 "1번 2번 해주고 … 바로 오늘부터"): 토 10:00 KST
  「주간 결산」(`docs/routine_week_review.md`, `editorial/weekly/review_<토요일>.json`, 시리즈
  "주간 결산")과 일 20:00 KST 「다음 주 일정」(`docs/routine_week_ahead.md`,
  `editorial/weekly/ahead_<일요일>.json`, 시리즈 "다음 주 일정")은 기준표 파이프라인을 그대로 쓰고
  `weekly_publish.yml`이 **바로 공개**한다. 분류는 Weekly(id 433, 목록 페이지 `/weekly/`, 홈 탭 —
  2026-09-12 사용자 "진행, 이름은 Weekly로"; 네이버에도 같은 이름). 지수·종목·환율·금리 숫자는 `python -m scripts.weekly_stats`
  (시세 파일의 3개월 이력으로 한 주를 센다)에서만 온다 — 검색 결과의 등락률을 옮겨 적지 않는다.
  그래픽은 `number_cards`·`movers_list`에 `"period": "week"`를 넣어 달력 주간 등락으로 그린다(휴장 주에도 전주 마지막 종가 대비). 문턱은
  주간 결산 절 5·시각자료 4·출처 2, 다음 주 일정 절 4·시각자료 3·출처 2
  (`feature_checks.SERIES_LIMITS`·`editorial_title.SECTION_FLOORS`·`source_check.SERIES_MIN_SOURCES`).
  머리말은 최상위 `period`로 `주간 결산 · 9월 7일~11일`. 거래일 3일 미만인 주는 결산을 쓰지 않는다.
  근거: 벤치마크(재테크농부)가 다섯 주말 24편 중 토요일 아침 미국장 주간 결산과 일요일 밤
  "이번주 투자 전략"을 5주 내내 같은 자리에 올렸다. 지난주 판정(맞음·빗나감)은 이 글에 넣지
  않는다 — 사용자가 아직 정하지 않았다.
- **유입 편성**(2026-09-12, 사용자 승인 "1번 2번 4번 진행, 3번 5번도 승인"; 근거는 서치콘솔 실측 — 구글 노출
  710건 중 700건이 영어 가이드 9편, 한국어 시황 43편은 0건): 새 시리즈 셋을 기준표 파이프라인에 표 한 줄씩으로
  등록했다 — **가이드**(한국어 상시, 화·목 09:00 KST, `docs/routine_guide_ko.md`, `editorial/guides/ko_<slug>.json`,
  분류 Guides 153, 머리말은 최상위 `checked` 날짜), **Guide**(영어, 수 09:00 KST, `docs/routine_guide_en.md`,
  `editorial/guides/en_<slug>.json`, `lang: "en"` — 관문이 한국어 검사 대신 영어 검사(`feature_checks.collect_issues_en`)와
  영어 출처 목록(`source_check.EN_SOURCES`)을 쓴다. **2026-09-13부터 매일 11:00 KST, 주 7편**이다 — 근거는 노출 710건 중
  700건이 영어 가이드에서 나온 것이고, 주제 60개를 아홉 갈래로 나눠 어제와 다른 갈래에서 고른다. 한국어 가이드는
  분류 **509**(`/guide/`), 영어는 **153**(`/guides/`)로 갈라 두었다 — 한 목록에 두 언어를 섞지 않는다.
  **한국어 가이드도 2026-09-13부터 매일 13:00 KST, 주 7편**이다(주제 59개를 열 갈래로) — 근거는 같은 날 실측:
  3개월 동안 구글에서 이름이 잡힌 검색어 21개가 전부 영어였고 한국어 시황 43편은 한 번도 노출되지 않았다.
  시황은 검색용이 아니라 브랜드·재방문용이고, 한국어에서 검색으로 이길 수 있는 것은 가이드뿐이다), **이벤트**(일요일 다음 주 일정 루틴이 그 주의 큰 이벤트 하나를
  `editorial/events/<날짜>_<slug>.json`으로 더 쓴다, 분류 Weekly, 머리말은 `event_date`). `guide_publish.yml`·
  `weekly_publish.yml`이 자동화 스위치대로 공개한다. 문턱은 가이드 절 5·시각자료 2·출처 2, Guide 절 5·2·2, 이벤트 절 4·2·2.
  **종목 허브 페이지** `/stocks/<slug>/` 37개(`config/stock_pages.yaml`, `src/stock_pages.py`, `stock_pages.yml` 토 11:30 KST):
  숫자는 시세 파일에서만, 「최근 흐름」은 매월 1일 루틴의 `editorial/stocks/notes_<YYYY-MM>.json`(`docs/routine_stock_notes.md`,
  `--check-notes` 통과 후 커밋). 홈·목록 탭에 Stocks. 네이버에는 한국어 가이드·이벤트 요약본이 간다(가이드 분류 공개) — **2026-09-22부터는 전문, 본진은 비공개**(아래 「발행 워크플로우」).
  「이 종목이 나온 글」은 **2026-09-26부터 네이버 주소로 잇는다**(사장님: "9번 제외 모두 고쳐"). 한국어 글을 본진 비공개로
  돌린 9/22에 성적표 주소는 네이버로 바꿨지만 종목 페이지는 빠져, 37쪽의 링크 179개가 방문자에게 404였다(삼성전자
  페이지 표본 5개 전부 404). 네이버 글 번호는 맥의 게시 기록에만 있어 맥이 `data/naver_posts.json`으로 옮겨 하루 한 번
  그 파일만 커밋한다(글마다 커밋하면 잡음). 표가 없으면 페이지 만들기가 멈춘다 — 빈 목록으로 조용히 가지 않는다.
- 그림을 그린 뒤에는 **`Read` 툴로 직접 본다.** 축을 한 종목이 독차지하거나
  이름이 막대와 어긋나는 것은 코드만 봐서는 안 보인다(둘 다 실제로 겪었다).
  `src/graphic_checks.py`가 그림의 데이터와 제목이 어긋나는 것을 잡지만,
  "제목은 반대라는데 다섯 줄 다 같은 방향"까지만 본다.

### 조용한 실패를 만들지 않는다

오늘 찾은 것이 여섯 개다. 전부 **실패했는데 성공처럼 보이던** 것이다.

- 단독 실행 모듈에 `load_dotenv()`가 빠져 있어 "업로드 안 함"으로 조용히 끝났다.
- 한글 폰트가 없으면 두부(□)로 그리고도 테스트가 통과했다(CI에 폰트가 없었다).
- 설정이 없으면 빈 dict를 돌려주고 "건너뜁니다"라고만 찍었다.
- 엔진이 예외를 삼켜 "못 받았다"와 "0건"이 같은 화면으로 나왔다.

**새 코드에서 `except: continue`를 쓸 때는 반드시 센다.** 세지 않을 거면 삼키지
않는다. 마찬가지로 `if not 설정: return`은 쓰지 않는다 — 예외로 올린다.

## 발행 워크플로우

- 새 글이든 기존 글 수정이든, 사용자가 명시적으로 "발행해"라고 하기 전엔
  워드프레스에 **임시저장(draft)**으로 올린다. 검수 후 상태를 `publish`로
  바꾼다.
- 같은 초안을 여러 번 고칠 땐 새 글을 또 만들지 말고
  `publish_wordpress.update_draft()` / `publish_guide.py`의 `post_id`
  인자로 같은 글을 덮어쓴다 — 임시저장 글이 중복으로 쌓이는 걸 방지.
- **오래된 원고 파일은 발행 전에 실제 라이브 slug를 조회해서 doc의 `slug`
  필드로 명시한다.** `publish_feature.py`의 `_slug()`는 `doc["slug"]`가
  없으면 파일명으로 slug를 만드는데, 파일명을 나중에 바꿨거나 처음부터 다른
  이름으로 발행된 원고는 이게 실제 글과 어긋난다. 2026-09-06에 이걸 놓쳐서
  기존 글을 덮어쓰는 대신 새 글(draft)이 중복 생성됐다 — 발행 후 결과의
  `id`가 예상한 값인지 반드시 확인한다.
- **헤더·푸터·`single` 같은 사이트 전역 템플릿은 글 하나가 아니라 사이트
  전체에 영향을 준다.** 건드리기 전에 반드시 사용자에게 먼저 확인한다.
  2026-09-06에 헤더에 내비게이션 메뉴를 추가했다가, 알고 보니 사용자가
  예전에 의도적으로 뺀 것이었다.
- **카테고리·태그는 이름으로 찾되, 같은 이름이 둘이면 발행을 멈춘다**
  (`publish_wordpress._get_or_create_term_id`). 2026-09-07까지 "Daily"가 둘(121·123,
  123은 Polylang이 켜져 있던 시절 영어용으로 생긴 잔재)이라 영어 시황이 날마다 다른
  카테고리에 들어갔는데 아무도 몰랐다. 123은 121로 합쳐 지웠고 Polylang은 꺼져 있다.
  첫 번째를 고르고 넘어가는 코드로 되돌리지 말 것.
- **검색어 태그는 `src/post_tags.py`가 글에서 뽑는다**(2026-09-12, 사용자: "검색어 태그가 너무 부족한 것
  아닌가" → "진행, 이미 올라간 글도 고쳐"). 고정어 + 원고의 `tags` + 본문에 나오는 종목(시황은 2% 이상 움직인 것) +
  주제어 사전 + 달(`9월증시`), 15개까지. 네이버 요약본(`scripts/naver_post.py`)과 워드프레스 한국어 글
  (`publish_editorial`·`publish_feature`)이 같은 목록을 쓴다. 그전에는 시황이 매일 같은 셋, Checkpoint는 0개였다.
  이름은 낱말 경계로 찾는다 — '소비자물가'에서 '비자'가 잡힌 적이 있다. 이미 올라간 글은
  `python -m scripts.retag_wordpress --apply`(워드프레스)와 `~/.market-brief-naver/retag_post.py`(네이버)로 고쳤다.
- **사이트맵은 워드프레스 기본(`/wp-sitemap.xml`)이다**(2026-09-12, 사용자 승인). Rank Math 사이트맵 모듈은
  새 글에 반응하지 않아 두 번 멈췄다(9/6→9/8, 9/8→9/12: 새 글 20편이 사이트맵에 없었다). 그래서 모듈을 끄고
  기본 사이트맵으로 바꿨다 — robots.txt의 Sitemap 줄도 바뀌었고 옛 `/sitemap_index.xml`은 404다. **다시 켜지 말 것.**
  네이버 서치어드바이저에는 새 주소를 제출했고, 구글 서치콘솔은 사용자 계정으로 제출한다. 새 사이트는 네이버가
  스스로 오지 않으므로 `~/.market-brief-naver/nsa_request.py`가 새 글마다 수집 요청을 넣는다(20분 동기화에 붙어 있음).
- **영어 사이트 SEO 정리**(2026-09-26, 사장님: "노출 늘리는 순서로 정리해줘" → "5번 빼고 모두 진행해 네이버는 만지면 안돼" →
  "코드 조각 올려 그리고 지난 33편도 다시 올려"). 실측: 공개 글은 전부 영어인데 홈 제목 "Fermata - 코스피·뉴욕증시 마감 시황, 매일
  저녁"·설명·`og:locale ko_KR`이 한국어였고, 한국어 글이 본진 비공개라 거의 빈 목록 페이지 셋(Checkpoint·가이드·Weekly)과 스레드
  연결용 페이지가 검색 등록·사이트맵에 있었고, 영어 시황에서 가이드로 가는 링크가 0개였다. 관리자 화면 로그인이 없어(앱 비밀번호만
  있다) 홈 설정은 Rank Math 개발자 문서의 필터(`rank_math/frontend/title`·`description`·`robots`, `rank_math/opengraph/facebook/…`)로
  Code Snippets 11번에 넣었다 — 사이트맵 페이지 52→48. 가이드 링크는 가이드 원고의 `tags`가 시황 본문에 나오면 점수(흔한
  꼬리표는 1/가이드 수)로 고른다. 종목 페이지는 한국어라 영어 글에서 걸지 않았다. 5번(글마다 AI 활용 표시)은 사장님이 뺐다 —
  잡지 블로그가 자동 생성으로도 노출이 잘 되므로 문제가 아니라는 판단. 개인정보처리방침 /privacy/·/privacy-policy/는 겹침이 아니라
  영문·국문 판이다.
- **종목 허브 페이지(`/stocks/`)를 없앴다**(2026-09-26, 사장님: "워드프레스에 stocks가 필요한가?" → "2번 완전히 없애기").
  근거: 서치콘솔 3개월 검색어 101개 가운데 종목 관련은 1개("삼성 생명 주가", 37위·노출 1회, 전체 노출 259회 중 1회), 영어
  사이트 안의 한국어 페이지 37쪽, 네이버 증권·야후에 이미 있는 종가·차트라 이기기 어렵고 같은 틀의 페이지 37개였다. 걷어낸 것:
  `src/stock_pages.py`·`templates/stock.html.j2`·`config/stock_pages.yaml`·`stock_pages.yml`(토 11:30)·`docs/routine_stock_notes.md`와
  월간 루틴(trig_01WDkBf1Nu9cLdrqWN5aM2KM, 꺼 둠)·네이버 주소표(`scripts/naver_map_sync.py`·`data/naver_posts.json`·맥 `naver_sync.naver_map`)·
  `site_audit --stocks`·홈과 목록 탭 7곳의 Stocks 링크·워드프레스 페이지 38개(휴지통, 30일 뒤 자동 삭제)·블로그스팟 글 끝 "종목
  페이지" 줄(블로거 라벨의 종목 이름은 워치리스트에서 뽑아 유지). 이미 올라간 블로그스팟 20편 중 13편의 그 줄은 같은 날
  맥의 `~/.market-brief-google/blogger_remove_stock_links.py`(편집기 HTML 보기에서 그 줄만 지우고 업데이트, 공개 글로 확인)로
  지웠다 — 사장님 "1번 고쳐야지", 다시 받아 보니 남은 글 0편. 비공개 한국어 글 네 편의 `related`에 종목 주소가 남아 있다(방문자에게
  보이지 않는다). 실제 방문 수(애널리틱스)는 보지 않고 정했다.
- **한국어 글을 본진에 아예 올리지 않는다**(2026-09-26, 사장님: "워드프레스에 비공개로 올라가는 한국어 컨탠츠를 지워도 되냐?" →
  "확인못한거 확인하고 문제없으면 둘다 진행해"). 9/16에 비공개로 되돌린 이유(네이버 사본이 `outlook`·`insight_section`·`sources`를
  빠뜨려 본진이 유일한 보관처)는 네이버가 원고를 빠짐없이 옮기게 되며(`test_naver_completeness`·`naver_audit`) 사라졌다.
  확인한 것: 맥의 `naver_sync`는 워드프레스 설정을 비우고 원고에서 만든다, 코드 조각 11개 중 비공개 글을 쓰는 것은 없다(6번 hreflang은
  짝이 공개일 때만), 공개 글·페이지·내비게이션·템플릿에서 비공개 글로 거는 링크 0, 비공개 글을 읽는 코드는 발행 확인 하나(스위치를
  끄면 한국어를 건너뛴다). Rank Math 리디렉션 목록은 공식 조회 방법이 없어 못 봤다 — 비공개 글은 방문자에게 이미 404라 결과가 같다.
  바꾼 것: `publish_editorial.KO_DAILY_TO_WORDPRESS=False`(한국어 그림도 안 올린다 — 본문·인사이트 **사진**은 영어판이 같이 써서
  올린다), `publish_feature.KO_TO_WORDPRESS=False`(`LIVE_STATUS`에서 `private`인 시리즈는 관문·렌더만 하고 업로드 없이 끝낸다),
  비공개 85편은 휴지통(30일 뒤 자동 삭제, 미디어는 블로그스팟 그림이 쓰므로 남긴다), 루틴 지시문의 알림 문구.
- **본진을 온전한 영어 사이트로**(2026-09-26, 사장님: "위클리 체크포인트 한글 가이드누르면 네이버로 링크되잖아 그거 다 뺴고
  온전히 영어사이트로 탈바꿈하자" → "남기고 1~5번 진행해" — 한국어 소개·개인정보처리방침은 남긴다). 한 일: 탭에서 네이버로 가던
  Weekly·Checkpoint·가이드를 빼고 전체→All, 빈 목록 1047·1439·1610 휴지통(처음 요청은 카페24가 502를 내 안 지워졌다 — 8초 간격으로
  다시), 날짜 형식 `F j, Y`, 머리말·꼬리말의 한국어 소개 문장 삭제, 아래 메뉴의 한국어 링크 문단 삭제, 템플릿 글자(이전/다음·작성자·
  검색 결과 없음·다른 게시물)를 영어로, 코드 조각 10번을 모든 글에 적용(옛 영어 글 두 편에 한국어 텔레그램 안내가 남아 있었다).
  사이트 언어(`language`)와 태그라인(`description`)은 REST로 보내면 200이 오고도 값이 그대로였다(`date_format`은 바뀌었다) — 원인을
  확인하지 못해 코드 조각 12번(`locale` 필터로 방문자 화면만 en_US, 태그라인 필터)으로 대신했다. 사장님 계정 언어는 ko_KR로 고정해
  관리자 화면은 한국어다. 남은 한국어: 글 57편 중 17편의 본문 — 9/1~9/14 영어 시황의 출처 칸에 한국 매체 기사 제목이 원문 그대로이고,
  몇 편에 고유명사·단위가 한글로 있다(원고를 고쳐 다시 올려야 해서 손대지 않았다). 백업은 `~/.market-brief-backups/en_site_*.json`.
- **영어 글에 남은 한국어를 0으로**(2026-09-26, 사장님 "모두 진행해"). 출처 칸은 원고를 고치지 않고 그리는 곳에서 바꿨다
  (`src/english_sources.py` — 매체 34곳 중 영문명이 알려진 곳만 적고 나머지는 사이트 주소, 기사 제목은 `Korean-language article`,
  링크는 원문). 편입 종목 9개의 `name_en`이 한글이던 것은 네이버에서 시장 구분을 받아 야후 회사명으로 채웠다(원고 6편),
  영어 본문의 한글 괄호 넷을 뺐다. 원고가 없는 옛 영어 글 2편(34·173)은 워드프레스 본문에서 원→won·배→x·사진→Photo. 가이드 1편은
  그사이 엄격해진 표 폭 검사에 걸려 칸을 줄였다. 발행 워크플로가 6편 중 4편을 올리고 카페24 시간 초과로 멈춰(실패 메일) 나머지는
  맥에서 20초 간격으로 올렸다. 사이트 언어가 안 바뀌던 원인도 찾았다 — 영어는 빈 값으로 저장하고 `en_US`는 거절된다(방화벽 추측은
  틀렸다). 공개 페이지 64개를 다시 세어 한국어가 보이는 곳 0.
- **블로그스팟 홈을 토스피드 풍으로**(2026-09-26, 사장님: "블로그스팟 홈페이지가 좀 허접해보이지 않냐?" → 참고 모음 두 번(구글 블로그·
  Plus UI 등은 "다 마음에 안드는데 더 찾아") → "토스피드처럼 만들어줘"). 흰 머리말(로고 왼쪽·메뉴·검색), 큰 추천 글 카드(사진 왼쪽·
  제목 오른쪽), "최신 콘텐츠" 두 칸 목록(글은 왼쪽·둥근 썸네일 오른쪽), Pretendard, 사막 사진·"Pages" 메뉴·공유·댓글 버튼·햄버거를 뺐다.
  유료 테마가 아니라 공식 테마 위에 스타일을 얹었다 — 11/30 디스커버 판정 중이라 구조는 두고 겉만 바꿨다. 이미 올린 20편은 새
  차례로 다시 만들었다(`blogger_refresh_order.py`, 그림 수가 줄면 건드리지 않음). 글 페이지 위에 붙던 기본 테마의 추천 글 카드도 감췄다.
  이어서 "너무 하얀색" → 색 시안 두 차례(첫 네 가지는 "뭔가 마음에 안드는데" — 흰 화면에 색만 덧칠했고 색이 할 일이 없었다) →
  두 번째 네 가지(토스피드 충실판·FT 연어빛·뉴스레터 크림·증권앱 다크, 전부 코너 색 이름표) 중 "C로 가자". 크림 바탕·검은 테두리
  카드와 그림자·노란 추천 카드·형광펜 밑줄. 코너 이름표는 `Blog1`의 `postTitle` 조각 앞에 라벨 줄을 넣고, 스타일이 코너·시리즈
  라벨만 보이게 한다(종목 라벨은 숨김). 날짜 앞 "- "는 게시물 위젯 설정 `timestampLabel`이 "-"라서였고 스타일로 감췄다. 추천 글에도
  이름표를 넣으려고 `FeaturedPost1`에 `snippetedPostTitle` 조각을 덮어썼더니 제목이 "Featured Post"로 바뀌어(조각이 받는 변수가
  다르다) 직전 백업으로 곧바로 되돌렸다 — 추천 글은 이름표 없이 둔다. 문서로 확인되지 않은 블로거 조각은 덮어쓰지 말 것.
- **공개된 한국어 글은 텔레그램 채널 `@fermata_kr`과 스레드 `@fermata.it.kr`에 자동으로 올린다**(2026-09-12, 홍보 1·2번 —
  `src/notify_telegram.py`·`src/notify_threads.py`, `publish_feature`·`publish_editorial`이 공개 직후 부른다). 표지 + 제목 +
  Take 두 문장 + 링크. 다시 올린 글(최초 공개와 수정이 10분 넘게 벌어진 글)과 영어 글은 보내지 않는다. 토큰은 GitHub 시크릿
  (`TELEGRAM_BOT_TOKEN`, `THREADS_ACCESS_TOKEN`, `THREADS_USER_ID`)과 로컬 `.env`에만 — 저장소에 적지 않는다. 스레드 토큰은
  60일짜리라 이 맥의 launchd(`kr.it.fermata.threadsrefresh`, 일 21:30)가 `scripts/threads_auth.py refresh`로 갱신한다.
  알림 실패는 발행을 실패시키지 않고 `[텔레그램 실패]`·`[스레드 실패]`로만 찍는다.
- **네이버 제목은 본진 제목 그대로다**(2026-09-17, 사장님: "날짜 시황 제목 앞에 쓰는거 삭제"). 2026-09-13부터
  9-16까지는 "네이버에서 검색해도 안 나온다"는 조사 끝에 검색어·날짜 머리(`코스피 마감 시황 9월 16일: …`)와
  `| 투자 체크포인트` 꼬리를 붙였다. 2026-09-17에 상위 블로그 제목 450개를 읽어 보니 **앞자리는 훅**이 차지해야
  하고(김무무·경제현미경은 앞 15자가 독자 목소리인데도 검색 1·2위), 검색어는 제목 어딘가에 있으면 됐다. 그래서
  머리도 꼬리도 뺐다 — `scripts/naver_post.naver_title()`은 본진 제목을 그대로 돌려주고, 이미 올라간 글은
  `~/.market-brief-naver/retitle_sync.py`로 맞췄다. **제목에 검색어를 넣는 일은 제목 문법의 몫이다**
  (`docs/editorial-style.md` 「제목 문법」). 네이버 검색 유입은 여전히 본진으로는 못 뚫는다(같은 날 실측:
  본진은 색인은 돼 있었지만 주제 검색에서 한 번도 안 잡혔다) — 네이버 쪽 검색 작업은 블로그에 건다.
  **제목 축 분포(2026-09-17)**: 우리 시황 28편 중 11편이 `A 3.2% 등락, 이유는 B입니다` 한 틀이었다(9/9 예문집의
  가장 쉬운 꼴만 골라 쓴 것). 예문집에 시간·질문·감춤 이유·독자·내 돈·1인칭·인용 축을 더했고(`editorial_title.AXIS_PICKS`),
  관문(`axis_issues`)이 최근 네 편에 없는 축을 이번 제목이 채우는지 보며 답 노출형은 다섯 편에 한 번만 둔다.
  1인칭 제목은 「판단」 절의 **대응 원칙**(추천이 아니라 조건이 붙은 우리 원칙)과 짝이다.
- **네이버에 가는 글은 본진과 다른 문장으로 다시 쓴다**(2026-09-12 가이드·주간부터, **2026-09-14부터 시황도**).
  2026-09-13 실측: 네이버 시황이 본진 글과 21.9~40.9% 겹쳤다(가이드 2.1%, 프리뷰 0.5%). 시황만 이 규칙에서
  빠져 본진 문장을 그대로 옮기고 있었고, 하필 하루 두 번으로 가장 자주 올리는 종류였다. 네이버는 이런 글을
  유사문서로 걸러 내고, 걸리면 **네이버 통로가 통째로 막힌다** — 같은 날 조사에서 본진은 주제 검색에 한 번도
  안 잡혔고 네이버에서 이길 수 있는 곳은 블로그뿐임이 확인됐다. 규칙은 `feature_checks.naver_spec()` 한 곳이다:
  가이드·주간 결산·다음 주 일정·이벤트는 절 4~6·1,200~3,000자, 시황은 절 3~5·900~2,200자(분량을 늘리는 것이
  목적이 아니라 겹침을 없애는 것이 목적이다). 25자 이상 같은 문장이 있으면 관문이 막는다 —
  기준표 계열은 `feature_gate`, 시황은 `editorial_gate`. 루틴 지시문은 `docs/routine_common.md`의
  「네이버용 본문」 절이고, 규칙을 바꾸면 그 문서도 같이 고친다.
  **전문이 가는 글(시황·프리뷰)은 본진과 같은 차례다 — 표지 → 본문 → Fermata's Take → 다음 확인 지점**
  (2026-09-15, 사용자 지적 "본문 형식으로 바꾸면 페르마타 테이크가 맨 아래로 가는거 아니었니"). Take를 두 문장만
  뽑아 맨 위 인용구로 올리는 것은 **요약본**(기준표·가이드·주간·이벤트)의 규칙이다 — 링크를 누르게 하려는
  장치이지 전문의 차례가 아니다. `tests/test_naver_post.py`가 양쪽을 다 고정한다.
- **Checkpoint도 본진에는 비공개로만 올라가고 네이버에 본문 전문이 나간다**(2026-09-15, 사용자: "체크포인트는 2번
  가이드는 3번으로 진행해"). 프리뷰와 같은 처리다 — `publish_feature.LIVE_STATUS`에 `"기준표": "private"` 한 줄.
  근거는 서치콘솔 실측이다: 구글 노출은 사실상 영어 가이드에서만 나왔고 Checkpoint는 0에 가까웠으니, 본진에
  공개로 남겨 두어 네이버가 유사문서로 거를 위험을 지는 쪽이 손해다. **가이드는 일부러 그대로 둔다** —
  구글 유입이 나오는 유일한 곳이라 같은 처리를 하면 안 된다(**2026-09-22에 뒤집음** — 구글 유입은 영어 가이드에서만
  나왔고 한국어 가이드는 0이었다; 아래 불릿). 같이 한 일 셋 — ① 이미 공개돼 있던 Checkpoint
  아홉 편을 비공개로 돌리고 네이버 아홉 편을 전문으로 다시 채웠다 ② 스위치가 켜지기 전에 나가 **공개로 남아
  있던 한국어 시황 일곱 편**(9/10~9/15, 네이버에 전문이 있는 것들)도 같이 비공개로 돌렸다 — 9/1~9/9치는
  네이버에 쌍둥이가 없어 그대로 둔다 ③ 성적표의 본진 주소를 네이버 주소로 갈아 끼웠다
  (성적표는 2026-09-26에 없앴다).
- **홈·목록의 Checkpoint 탭은 네이버 블로그 카테고리로 간다**(2026-09-15, 사용자: "2"). 본진 Checkpoint를
  전부 비공개로 돌려 `/checkpoint/` 목록이 비었는데, 탭을 지우면 되돌릴 때 여섯 군데를 다시 고쳐야 한다.
  그래서 탭은 그대로 두고 `href`만 `https://blog.naver.com/fermata49?Redirect=Category&categoryNo=6`으로
  바꿨다(`target="_blank"`). 고친 곳은 **페이지 6개(76 daily·77 guides·105 all·1047 checkpoint·1439 weekly·
  1610 guide) + `twentytwentyfive//home` 템플릿** — 이 일곱이 탭 줄이 복제돼 있는 전부다(REST로 전수 조회해
  확인했다. 724 scoreboard·1479 stocks의 'Checkpoint'는 본문 글자이지 탭이 아니다. 724는 2026-09-26에 성적표와 함께 지웠다). `flex-wrap`을 건드리지 말 것 —
  모바일에서 탭 줄이 두 줄로 접혀야 가로가 넘치지 않는다(390px 실측). `/checkpoint/` 페이지 자체는 살려 두되
  `query-no-results` 블록의 "아직 글이 없습니다"를 네이버 안내로 바꿨다 — **목록이 다시 채워지면 그 안내는
  저절로 사라지므로** Checkpoint를 다시 공개할 때 되돌릴 것이 없다. 네이버 카테고리 번호는 추측하지 말고
  `PostList.naver?blogId=fermata49`를 받아 `categoryNo=`로 확인한다(시황 1·Checkpoint 6·가이드 7·Weekly 8).
- **한국어 글은 전부 본진 비공개·네이버 전문이다**(2026-09-22, 사장님: "네이버 시황 블로그에 워드프레스 링크가 붙는
  컨텐츠에 링크를 모두 빼고 본문을 공개하고 워드프레스 본진에서는 비공개 처리해라 — 네이버는 한글 컨텐츠를 주력으로
  해야 한다"). 시황·프리뷰·Checkpoint에 이어 **한국어 가이드·주간 결산·다음 주 일정·이벤트**도 같은 처리다 —
  `publish_feature.LIVE_STATUS`에 네 줄, `naver_post`는 잡지를 뺀 모든 글을 `ko.narrative` 전문·링크 없음으로 옮기고,
  `feature_checks.NAVER_FULL_ENDED`(2026-09-23)부터 네이버용 본문(`naver`)을 요구하지 않는다. **영어 가이드("Guide")만
  본진에 공개다** — 2026-09-22 실측으로 구글 28일 클릭 21건이 전부 영어 가이드였다. 같이 한 일: 본진의 한국어 가이드
  11편·Weekly 5편을 비공개로, 네이버의 그 16편을 전문으로 다시 채우고(링크 제거) `naver_audit`로 대조, 가이드·Weekly
  탭을 네이버 카테고리 7·8로(Checkpoint 탭과 같은 방식, 같은 일곱 곳), `/guide/`·`/weekly/`의 빈 목록 안내를 네이버로.
  **근거는 2026-09-22 전수 검사다**: 네이버 검색에 fermata49 0/52·잡지 10/28(최근 이틀 6/6) — 자동 생성이 아니라
  fermata49의 이력(본진 중복·재작성 77회)이 문제였고, 사장님이 네이버를 한국어 주력으로 정했다. 되돌리려면 `LIVE_STATUS`
  네 줄을 지우고 `naver_post`의 `full_body = not magazine`을 옛 조건으로 되돌린다.
  **2026-09-26 시황 블로그 게시 멈춤.** 전수 재검사에서 fermata49 0/62(대조군 6/6, 잡지 최근 5/5)로 9/22와 같았다. 같은 날
  조사(네이버 공식 문서·실제 복구 사례): 네이버는 '저품질'이라는 말은 부정하지만 출처 단위 페널티가 쌓여 "아예 노출 제외"될 수
  있고 글을 지워도 이력이 남는다고 적는다(검색 공식 블로그 2019-06-25). 검색 노출 제보는 "개별 답변하지 않는다"는 통로라, 매일
  넣던 제보(`kr.it.fermata.naverreport`, 누적 15건)를 사장님 지시("지금 자동으로 제보 넣는거 멈춰줘")로 끄고, 서로이웃 자동
  신청(`kr.it.fermata.buddyrequest`, 9/25~26 21건)도 껐다. 사장님이 이용제한 고객센터(블로그 게시물 해제 요청, 본인인증)에
  문의를 넣은 뒤 "네이버시황만 글 올라가는걸 중단한다" → 맥의 `naver_sync`가 `fermata49_pause.json`을 읽어 fermata49로 갈
  원고를 건너뛴다(잡지는 그대로). 블로그스팟 글 끝의 "시황·프리뷰는 네이버 블로그에 올라갑니다 — 이웃 추가" 줄도 뺐다(새 글이
  가지 않는 곳으로 안내하지 않는다). 남은 길은 문의 답을 본 뒤 새 블로그로 옮기는 것 — 옮길 때는 옛 글을 옮기지 않고, 두 계정이
  이웃·댓글·링크를 주고받지 않는다.
- **블로그스팟(fermata49.blogspot.com, "Fermata 매거진")은 구글용 한국어 창구다**(2026-09-25, 사장님: "블로그스팟에 블로그를
  개설했다 어떻게 운용할지 최고의 제안을 해봐" → "유입과 수익을 극대화 하는 용도로" → "잘모르겠다 결정을 못하겠네 … 시너지 효과" →
  "진행해"). 근거: 서치콘솔 실측(노출 710건 중 영어 가이드 700, 한국어 시황 43편 0)이라 시황은 검색용이 아니고, 한국어 글은 본진
  비공개·네이버 전문이라 구글에 우리 소유의 한국어 사본이 없었다. 게다가 blog.naver.com/robots.txt가 AI 검색 봇(GPTBot·
  OAI-SearchBot·PerplexityBot·ClaudeBot·CCBot 등)을 전부 막아 우리 한국어 글은 AI 검색 어디에도 없었다 — 블로그스팟이 유일한
  사본이 된다. 같은 날 저녁 관리 화면을 읽어 보니 사장님이 애드센스 심사를 이미 신청해 둔 상태(글 0편)였고, 테마는 베트남 앱
  크랙 사이트용 무료 템플릿(VTrick: Download·Android 메뉴, 후원 광고, "Premium MOD APK" 링크, "activation tools" 문구)이라 심사
  탈락감이었다 → 공식 테마 Contempo Light로 교체, 맞춤 robots.txt(Yeti 차단·/search 제외), 로봇 헤더 태그(홈·글 all, 자료실·검색
  noindex), 서치콘솔 속성 자동 확인. 그림 호스팅은 깃허브 페이지(공개 저장소 kimwhey21/kimchi의 별도 가지)를 켜려 했으나 자동
  모드 분류기가 "공개 표면 생성"으로 막아 본진 미디어 라이브러리로 갔다(정적 파일이라 카페24 부하는 작지만 **일일 트래픽 한도**는
  확인 못 함 — 디스커버가 터지면 본진이 먼저 막힐 수 있다). 글 게시도 같은 분류기가 막아 공개 스위치 파일을 두고 첫 공개는 사장님
  손으로 켜게 했다. 밀린 원고 69편(잡지 39·가이드 14·Checkpoint 11·주간 4·이벤트 1)을 하루 10편씩 올린다. 그림 모음판·번호 붙이기
  같은 네이버 규칙은 그대로 따른다(`scripts/naver_post.build`를 재사용). 유사문서 위험은 Yeti 차단으로 막고, 구글이 네이버 사본과
  블로그스팟 사본 중 어느 쪽을 원본으로 고르는지는 4주 뒤 서치콘솔 「중복, 다른 표준 페이지 선택」에서 본다.
- **네이버 글이 원고를 다 담았는지 두 곳에서 확인한다**(2026-09-16). `tests/test_naver_completeness.py`는
  만들어지는 원고를 절·outlook·insight·표·Take·확인 지점·출처까지 하나씩 세고, 그림이 **자기 절 밑에**
  붙는지와 4장 상한이 되살아나지 않았는지 본다. `python -m scripts.naver_audit`은 **네이버에 올라간 화면**을
  받아 같은 대조를 한다(빠지면 0이 아닌 값으로 끝난다). 둘 다 필요하다 — 2026-09-15에 코드는 맞았는데 이미
  올라간 글은 옛 서식이었다. **`chars > N` 같은 바닥값을 완결성 검사로 쓰지 말 것**: 그때 있던 `chars > 2600`은
  옛 요약본 상한보다 낮아 28% 결손을 그대로 통과시켰다. 이미 올라간 글의 제목은 `~/.market-brief-naver/retitle_sync.py`가
  맞춘다 — `refill_all.py`는 본문만 다시 채우므로 본진 제목이 바뀌면 네이버 제목만 옛것으로 남는다(실제로 한 편 그랬다).
- **두 번째 네이버 블로그는 잡지다**(2026-09-13, 사용자: "blog.naver.com/jeunkim처럼 다양한 분야의 글을 잡지처럼").
  시리즈 `매거진`, 원고는 `editorial/magazine/<날짜>_<slug>.json`, 루틴은 `docs/routine_magazine.md`(매일 02:00 KST에 세 편 집필 — 다른 루틴이 없는 시각, 게시는 이 맥이 07:30·12:30·19:30 한 편씩,
  코너 일곱: 시장 읽기·시장의 역사·투자 심리·기업과 기술·과학·돈의 상식·만약에). **워드프레스에는 가지 않는다** — 발행 워크플로가
  없고 이 맥의 `naver_sync.py`가 `blogs.json`대로 두 번째 블로그에만 올린다(아이디가 비어 있으면 건드리지 않는다).
  참고 블로그 실측(이웃 11만·글 2만·하루 14편): 제목 → 📌 간단 브리핑 → 본문 → 자료 출처. 그쪽 엔진은 외국 기사 전문
  번역인데 그건 저작권 문제라 따라 하지 않는다 — 꼴만 가져오고 본문은 출처 둘 이상을 종합해 우리 문장으로 쓴다.
  그래서 잡지의 출처 검사는 금융 이름 목록이 아니라 원고 `sources` 가운데 **본문에 이름이 실제로 나온 것**만 센다
  (`source_check.collect`), 첫머리에 '번역'이 있으면 막고, 사진 한 장(`featured_photo.url`)을 요구한다(`feature_checks.magazine_issues`).
  기준표용 '확인 날짜' 규칙은 잡지에 걸리지 않고, **간단 브리핑도 Fermata's Take도 없다**(사용자 결정, 같은 날 저녁) — 표지 → 본문 → 자료 출처만. **브랜드는 퍼플썸이다** — 잡지 글의 태그·본문에 페르마타가 나가면 안 된다(고정 태그 `퍼플썸매거진`). 형식 예시는 `tests/fixtures/magazine_sample.json`(관문 통과본).
- **고친 것이 있으면 곧바로 커밋·푸시한다**(2026-09-13, 사용자: "이제 상황에 따라 수정이 있을때 자동 커밋 푸시 해라").
  그전 규칙("사용자가 명시적으로 요청할 때만")은 폐기했다. 이유는 클라우드 루틴이 규칙과 관문 코드를 **깃허브에서**
  받아 가기 때문이다 — 맥에서만 고쳐 두면 루틴은 옛 규칙으로 계속 돌고, 고친 사람만 고쳐졌다고 믿는다.
  대상은 `main`이다(작업 브랜치를 만들지 않는다 — 발행 워크플로가 `main` 푸시로만 돈다).
  - **푸시 전에 `python -m unittest discover -s tests`를 돌려 통과한 것만 올린다.** 실패하면 올리지 않고 보고한다.
  - **`editorial/**`·`config/stock_pages.yaml` 같은 발행 트리거 경로가 섞였는지 먼저 본다**
    (`.github/workflows`의 `paths`). 섞였다면 그 커밋은 글을 공개시키므로, 의도한 발행이 아니면 나눠서 올린다.
  - 비밀값(`.env`, 토큰)과 `output/`·벤치마크 코퍼스는 어떤 경우에도 커밋하지 않는다.
  - 커밋 메시지는 무엇을 왜 바꿨는지 한국어 한 줄로 적는다.
